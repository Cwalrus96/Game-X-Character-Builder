# Authoring display and bulk formatter

## Current configuration: source schema v5, September 22, 2026

The canonical [game-x-class-data](https://docs.google.com/spreadsheets/d/1TEdxuufglP8lFRNk8QD4N_351-0ihAUFLG2743ESjoI/edit) workbook owns the simplified authoring format. The [Game-X-Data-Display](https://docs.google.com/spreadsheets/d/106wXA3w52aubp0zCYqieHJME02C0bu4jdho9b_eBA8U/edit) workbook adapts that format for its existing display tabs and handbook bindings. This is an authoring/display migration; runtime import support and the frozen production release remain separate work.

The September 21 source readback contains 147 Techniques with 30 columns, 55 Traits with nine columns, and 19 Classes. All 43 existing native checks and the 16 new native cleanup fixtures passed after installation, and the bulk formatter completed successfully. These are checkpoint counts, not required future table sizes. See [the current status](../../docs/status.md) for final provenance, preservation evidence, unresolved boundaries, and later verification. Historical handbook acceptance below does not establish a new handbook refresh for this source revision.

`authoring-formulas.json` records cell formulas, fixture labels/expected values, required helper-sheet identities, and 13 named-function definitions. Generate it from the checked-in formula builders:

```powershell
node scripts/display/update-authoring-formulas.mjs
```

This command updates the local operational manifest only. It does not write either workbook, publish runtime data, or refresh the handbook. `technique-display-formulas.mjs` generates the Technique adapter, keyed renderer, prerequisite/pumping text, eight Technique named-function patches, and native fixtures. `schema-display-adapters.mjs` supplies the other adapters and five shared/option-group named-function patches. Native Sheets calculation remains the acceptance check; Node tests validate generator structure and local adapter behavior, not the Sheets calculation engine.

### Source fields and compatibility columns

The 30 authored Technique columns are independent of the 36 columns in `_Techniques!A1:AJ1000`. The adapter reads source headers by name and supplies correctly sized blank arrays for removed or absent optional fields. It preserves downstream positions, including name in A, selection in the historical `skill` slot C, rank in D, rank notes in AA, stable key in AC, and `associatedSkill` in AJ. The first 35 header labels remain historical compatibility labels; Function_Tests row 39 verifies those positions. They are not 35 required source headers.

| Canonical authoring field | Display compatibility slot | Meaning |
| --- | --- | --- |
| `selection` | `skill` | Skill alternatives, `granted`, `tag=…`, or `weaponTag=…` determine access. |
| Removed `status` | Empty `selectionMode` slot | Readiness uses required cells; the empty slot preserves downstream bindings. |
| `pumpingByRank` | `pumpDamageByRank` | Explicit rank/effect maps can grant damage, healing, wards, armor, or multiple effects. |
| `associatedSkill` | `associatedSkill` | Overrides the roll skill; a blank uses the selected skill or the provider's associated skill. |
| `prerequisites` | `prerequisites` | Formal source DSL is retained and rendered into readable text. |
| Removed fields | Empty historical slots | `damageByRank`, `notes`, `sourceNote`, `prerequisiteText`, `skillKeys`, and `tagKeys` are not authored again. |

Damage, including irregular progression, is authored in `damage`. Keep independent higher-rank benefits and restrictions in `rankNotes`. `strainCost` and `onCriticalFailure` remain supported. Mechanical content from removed notes belongs in descriptions or outcomes; the display does not recover mechanics from archived editorial notes.

Costs retain explicit fixed, variable, and conditional meanings; `energyCostOptions` preserves named alternatives. Blank Energy means zero. No `-1` or `N` compatibility sentinel stands in for a cost. Pumping renders only from explicit `pumpingByRank` entries and never from cost kind. Only missing required Technique identity, selection, rank, action type, or action count produces an incomplete notice. Optional Energy and pumping never do. Classes and Origins project empty historical status positions, preserving downstream column bindings after source removal.

The three Class skill fields retain their separate meanings in canonical `Classes`: `combatTechniqueSkill`, `combatSkills`, and `utilitySkillOptions`. Display adapters preserve their independent positions and do not derive their source from `ClassSkills`. Other adapters retain removed optional fields such as `grantNotes` as blank compatibility columns. `WeaponBases` retains a ten-column display view, including an empty historical `tagKeys` slot, without requiring that duplicate source column. The retired `WeaponProfiles` display helper is header-only.

### Stable-key rendering and formal prerequisites

Every Technique is rendered once in hidden `_TechniqueBlocks` (sheet ID `919260002`). Column A holds stable keys, B holds a specific weapon owner or blank, and C holds complete markdown blocks in source order. F:G projects the key and unchanged formal prerequisites from `_Techniques`; it no longer performs an independent canonical import. The helper does not end in `_Display`, so the formatter creates no separate output tab for it.

The renderer finds the source row by `techniqueKey`, then reads its name and all other fields from that row. Duplicate display names are valid. Missing or duplicate Technique references produce an explicit diagnostic for that record rather than breaking valid neighboring Technique blocks. An absent optional header produces blanks; unresolved identity is not treated as missing optional content.

The eight Technique named functions retain historical argument names, but an argument named `name` now carries a stable **key**. `TECHNIQUE_BLOCK` reads the independently generated pool. Cost, metadata, and cantrip/pumping helpers extract their corresponding lines from that shared block rather than maintaining a second prerequisite or pumping implementation. The usage helper reads keyed fields and honors `associatedSkill`. `TECHNIQUE_BLOCKS_BY_SKILL` maps keys, and its catalogue wrapper retains historical grouping behavior. The actual main catalogue uses the explicit filtering described below.

Formal prerequisites generate display text for weapon tags and alternatives, exclusions, reach, counts, wielding and separate-hand requirements; character tags; stable Technique, Trait, feat, class, and archetype references; and authored `text | text=…` requirements. Weapon and character tags remain different conditions. Unknown clauses retain their wording in an explicit diagnostic. Referenced entity names come from keyed source rows. Skills and tags use readable authored vocabulary without a second maintained slug column; entity relationships still use stable keys.

### Pumping and catalogue layout

Write every pumping rank/value once with explicit effects. For example, `1=+1 healing per Energy;2=+1 healing per Energy;4=+2 healing per Energy` renders as `Pump (healing per energy): +1 at ranks 1–2; +2 at rank 4.` Only consecutive ranks with equal coefficients and matching units form a run. Gaps and later repetitions remain separate. Mixed-unit or simultaneous effects use a generic `Pumping:` line and retain the whole effect. Ward/wards spelling is normalized for grouping. The display never assumes an unlabelled value is damage or reconstructs omitted ranks. Base-specific techniques label pumping with weapon ranks.

`Techniques_Display!A1` alphabetically sorts pool records whose owner in B is blank. A Technique is base-specific only when its formal prerequisites contain `weapon | key=<weaponKey> | wielded=true` and that base's `techniqueKeys` includes it. `selection=granted` alone does not exclude an entry. The existing 31 base-specific attacks/alternatives remain outside the main catalogue; generic techniques such as Covering Fire remain in it even when also provided by a base.

Excerpt columns D/F/H/J/L resolve keys against the complete pool, and N contains the base-specific excerpt. Weapon cards embed complete blocks from that same pool; their six rank excerpts derive from the card catalogue. Keep these columns and output sheet IDs because handbook bindings use them. Blocks retain the bold title, italic Access line, metadata, known roll, costs/trigger, targeting, description, outcomes, pumping, and rank notes. Granted/tag routes show an inherited `Associated skill` roll label when no override is authored. Cards omit repeated automatic-availability paragraphs while preserving restrictions and meaningful mechanics.

### Traits

The nine canonical Trait columns are `traitKey`, `name`, `rank`, `prerequisites`, `tags`, `description`, `rankNotes`, `techniqueKeys`, and `grants`. The existing 12-column hidden import grid may remain wider than the source; its width is not a source requirement. Optional historical metadata still renders if supplied and otherwise becomes blank arrays.

Trait `tags` classify the Trait. Structured `grants` identify tags acquired by its recipient and their minimum ranks, for example `tag | tag=Wings | minRank=1` and `tag | tag=Flight | minRank=2`. They do not add a duplicate benefit paragraph to the display. Trait rank follows the granting feature's associated skill or Familiar rank; source `rank` is the minimum tier. Origin Traits may grant fixed benefits without a skill.

Cards retain the bold title, bold Prerequisites/Tags labels, blank line, description, and rank notes. Formal prerequisites use the same formatter as Techniques. Blank prerequisites display `None`; blank tags display an em dash. Blank rank or draft metadata retains the incomplete notice. Each `techniqueKeys` reference must resolve uniquely. Duplicate Trait identities or dangling associated Technique references still fail the Trait renderer and stop formatting; Technique per-record diagnostics do not weaken this separate validation boundary. The description names associated actions in prose without another Techniques list or embedded blocks. Keep source ordering, provider relationships, and `Traits_Formatted` sheet ID `920260004`.

### Installation and verification

1. Regenerate and review the local manifest. Source edits require their separately approved exact scope; display installation does not authorize source mechanic changes.
2. Install adapters before consumers: `_Techniques!A1`, pool F1/A1, B1/C1, keyed named-function patches, then dependent formulas. Preserve helper/output IDs and excerpt columns. The archetype import stays independent of `_Feats` to avoid a cycle.
3. Wait for imports/recalculation. Check all 43 existing Function_Tests and the 16 native fixtures in K2:N17. Fixtures cover optional-header dimensions, duplicate names, key failures and valid neighbors, pumping gaps/units/simultaneous effects, formal prerequisites, unknown mechanics, inherited skills, strain, and critical failure. Review the identity diagnostic area as well as PASS counts.
4. Run **Update all Display → Formatted tabs**, then compare calculated text with formatted text and rich-text emphasis. Native fixture success alone does not verify formatting or linked handbook content.
5. When refreshing the handbook, use the managed populated-row workflow or update affected native links while preserving identities and formatting. Historical fixed ranges must not truncate added rows. Compare text and non-whitespace bold/italic styling; use **Match spreadsheet data and formatting** when a native linked table has Docs overrides.

`DisplayBatch.gs` uses the existing Apps Script menu, markdown/rich-text, sizing, and width helpers. It discovers all `_Display` tabs, including hidden ones; flushes changes; and stops before output mutation if source cells contain formula errors or loading indicators. This does not force `IMPORTRANGE` refresh. It reuses output IDs, preserves internal gaps/excerpts and relevant formats, and uses a document lock. It introduces no service, permission scope, timed trigger, or source write. Handbook links need their own subsequent refresh.

## Historical September 18–20 implementation and acceptance notes

Everything below describes earlier source versions and checkpoints. In particular, the old 12/eight-column Trait formats, required 35-header Technique import, cost sentinels, `damageByRank`/`pumpDamageByRank` authoring instructions, and editorial-note rendering have been superseded by schema v5 above. Historical counts and linked-handbook acceptance do not verify later source changes.

The later September 20 consolidation used eight core Trait columns, with blank arrays for missing optional metadata and native missing-header/reference fixtures. An earlier September 20 checkpoint verified 77 formatted Trait cards and 120 general Techniques against linked handbook text and emphasis, with all 39 then-current checks passing. The nine-column source and 43-plus-16 verification above are the current checkpoint.

`DisplayBatch.gs` extends the existing bound [Game X project](https://script.google.com/u/0/home/projects/1RfzvZR0z7ytWstW5e6EnXEpP90dqu8muhSz5Kojv4k1mRBvrTRBgrht2/edit) for [Game-X-Data-Display](https://docs.google.com/spreadsheets/d/106wXA3w52aubp0zCYqieHJME02C0bu4jdho9b_eBA8U/edit).

The existing `Code.gs` owns markdown parsing and individual-tab formatting. Add this item to its `onOpen()` menu chain, before the existing items:

```js
.addItem('Update all Display → Formatted tabs', 'generateAllFormattedOutputTabs')
.addSeparator()
```

The new function finds every tab ending in `_Display`, case-insensitively, including hidden tabs. It flushes pending spreadsheet changes, reads current calculated display values, validates every source, and converts all outputs using the existing `buildMarkdownRichTextGrid_`, `ensureSheetSize_`, and `copyColumnWidths_` helpers. It stops before changing outputs if any source contains a formula error or loading indicator. This does not force an upstream `IMPORTRANGE` refresh; imports must have finished recalculating before the command runs.

Existing output tabs are reused without changing sheet IDs. Missing outputs are created, matching the source's hidden status. Empty formula tails are trimmed while retaining internal blank rows/columns and all technique excerpt columns. Each output receives the existing markdown styling, source cell formats/column widths, wrapping, frozen rows/columns, and automatic row sizing. The command uses a document lock, restores the originally active tab, and reports completion or partial failures.

No new service, permission scope, timed trigger, or source-workbook write is introduced. The handbook's native linked tables still need **Update all** in Google Docs after this spreadsheet command completes.

`authoring-formulas.json` records the current authoring display configuration. It is an operational reference, not an automatic migration or runtime export. Its `requiredSheets` entry describes the hidden `_TechniqueBlocks` helper (sheet ID `919260002`), `cells` contains formulas, and `values` contains the new Function_Tests labels. `_ArchetypeFeats!A1` derives membership directly from canonical Feats, avoiding a circular dependency with `_Feats`' prerequisite renderer.

Every canonical technique is rendered once in `_TechniqueBlocks`, including unfinished techniques with blank rank/access. Column A contains stable keys, B contains a specific weapon owner or blank, and C contains the complete markdown blocks, all in canonical source order. F:G imports canonical technique keys and raw prerequisite DSL together from one `IMPORTRANGE`; this retains the machine-readable prerequisite that `_Techniques` converts into display prose. The helper's name deliberately does not end in `_Display`, so the bulk formatter creates no additional formatted tab for it. Install F1/A1 first, then B1/C1, and wait for imports and calculations before updating consumer formulas.

`_Techniques!A1` preserves the existing cost and prerequisite compatibility conversions, then selects all 35 output columns by header name in the established display order. This keeps name in A, description in B, skill in C, rank in D, rank notes in AA, source note in AB, and stable key in AC even when canonical columns move. Install this adapter before refreshing `_TechniqueBlocks` or its consumers; do not reorder source cells to repair a display dependency. Function_Tests row 39 requires all 35 compatibility header positions to match before formatting. Native calculation remains the acceptance check for this formula.

The universal Trait pipeline adds the hidden `Traits` import, `Traits_Display`, and `Traits_Formatted`. The canonical authoring columns are `traitKey`, `name`, `rank`, `prerequisites`, `tags`, `description`, `rankNotes`, `techniqueKeys`, `selectionMode`, `duplicateGroup`, `reviewNotes`, and `sourceNote`. Import `Traits!A1:L1000` before installing the display formula, and wait for the canonical technique pool to include any new technique keys. Existing `generateAllFormattedOutputTabs` discovers the new pair automatically; no new bound-script code or menu action is required. Keep the configured output sheet ID when creating the linked handbook table.

Trait blocks use a bold `Name - Rank N` title, separate lines with bold `Prerequisites:` and `Tags:` labels, a blank line, and the authored benefit text and `Rank N+` notes without extra labels. Blank prerequisites mean `None`; blank tags render an em dash to show that no tags were authored. Preserve uncertain prerequisites explicitly in source text or review notes. A blank rank renders `Rank ?`, and blank-rank or draft entries retain an italic `Incomplete Trait` notice. Duplicate-group labels and review notes are italic; `sourceNote` remains internal provenance. Each nonblank review-note line is styled independently, and source ordering and rank-note text remain intact.

`techniqueKeys` validates the associated canonical references against `_TechniqueBlocks`; each must resolve exactly once. Missing or ambiguous references and duplicate Trait keys cause a formula error, so the bulk formatter stops before changing any output. The Trait description names its associated techniques in ordinary prose. The display does not append a separate Techniques label or embed technique blocks, and those techniques stay in the existing catalogue unless the independently defined weapon-specific rule excludes them. Trait benefits, choice rules, scaling, and technique-access meaning remain authored source content rather than inferred display policy.

Function_Tests rows 34–38 check source/display coverage, formatted coverage, unique technique-reference resolution, preservation of all nonempty canonical rank notes, and the heading/incomplete-notice contract. Native calculation and rich-text readback are still required after installation: local formatter tests verify pair discovery, multiline-content preservation, stable output IDs, and stopping on a Trait reference error, but do not execute Google Sheets formulas.

`Techniques_Display!A1` alphabetically sorts only pool records whose owner in B is blank. A technique is base-specific only when its raw prerequisites contain the exact canonical line `weapon | key=<weaponKey> | wielded=true` (allowing surrounding whitespace) and that same base's `techniqueKeys` list contains the technique key. Neither base membership alone nor `selectionMode=granted-only` is sufficient. This excludes the 31 base-specific attacks/alternatives while retaining generic techniques such as Covering Fire in the main catalogue, even when a base also provides them. Excerpt columns D/F/H/J/L resolve directly against the complete keyed pool, and N derives all base-specific blocks from it; none depend on membership in the filtered main catalogue.

Complete techniques retain the existing rendering and rank-scaling compaction. Drafts preserve any stated rank, attack roll, action count, strain, sustained state, and reaction trigger, while missing actions and unassigned energy remain explicitly unassigned. The roll helper runs independently of the action/cost helpers, because known roll details can coexist with unknown action costs. Its empty `() .` result is omitted. The combined action/cost helper runs only when both action type and count are present; a known Energy cost with unknown actions is rendered separately, preventing the helper from inventing a free action. All rank scaling remains canonical technique text; the display does not recalculate weapon damage or energy rates.

Field-presence checks read each named field with `LEN(TO_TEXT(TECHNIQUE_GET_FIELD(...)))`, so empty strings spilled by the import adapter remain missing and numeric zero remains present. Do not use `COUNTIFS` nonblank criteria for rank, action type/count, or strain: formula-generated empty strings can satisfy those criteria. Function_Tests row 40 checks all 14 Trait-associated draft additions for explicit unknown-action/rank notices and the absence of empty Actions or Strain lines.

For future techniques, follow the compact Spirit Blast and Spacium Ray convention. Put starting damage and linear growth in `damage`, naming the rank basis and starting rank; for example, `5 + Hits; +3 damage per weapon rank above 1.` Leave `damageByRank` blank for this linear progression. Preserve explicit maps for genuinely irregular damage; the renderer does not infer a formula or rank basis from those maps. Keep independent higher-rank effects and meaningful restrictions in `rankNotes`, without repeating the damage, pumping progression, or zero-Energy base cost. The four Rank 0 basics retain `No pumping at rank 0.`; the redundant `0 Energy before optional pumping.` sentence is removed from all 23 basics.

Keep `pumpDamageByRank` as canonical data. The shared renderer groups equal rates only across consecutive ranks, preserving gaps, rate changes, and damage/armor/ward units. It compacts a bare zero such as `Rank 0: +0` only when that exact rank has a zero entry in the canonical pumping map and other pump entries establish one consistent unit. Mixed-unit or unrecognized lines retain their original text. Base-specific blocks label pumping with `weapon rank` or `weapon ranks`; ordinary techniques retain their existing rank labels. The same shared formula handles newly added techniques without a per-technique display edit. Native README rows 19–20 record this authoring default, and Function_Tests rows 31–33 check the two reference techniques, compact basic weapon attacks, and preservation of every nonempty canonical rank note. Native acceptance passed for this compact-format follow-up: 69 source cells migrated all 23 basics, every previous damage-map value remains equivalent to its compact expression, and all pumping maps remain unchanged.

Weapon cards and their embedded technique blocks omit repeated automatic-availability sentences, including the draft availability paragraph. Drafts still show `Incomplete technique`. The internal weapon owner remains available for catalogue filtering, mechanical-note inclusion, and weapon-rank labels; removing printed repetition does not change access. Mechanical notes retain reload restrictions; the known editorial prefixes `Draft:`, `Attack text lists `, and `Current handbook damage ` remain source-only. Technique column L contains the 14 surviving entries from the earlier weapon excerpt, preserving the user's deletion of `brutal-strength` and `bullet-spray`. Column N remains available as a 31-block excerpt. Function_Tests compares calculated and formatted coverage through column N; row 7 compares source key count with the complete pool instead of the reduced main catalogue. Rows 26–30 check pool rendering coverage, general-catalogue separation, base-specific excerpt coverage, exact full-block inclusion for every base relationship, and formatted base-card coverage. Row 32 additionally rejects the removed availability/zero-Energy lines while requiring compact scaling and Rank 0 pumping restrictions; row 33 continues to preserve every meaningful canonical rank note.

The repetition cleanup passed native acceptance. Only 23 canonical `rankNotes` cells changed: 19 are blank and four retain the Rank 0 restriction. All 32 spreadsheet checks pass; the three redundant sentences are absent from the 139-block pool, 31 base cards, and 153 linked handbook entries. All 153 handbook entries match their current formatted source text and bold/italic styling, and the Longsword card passed native visual review. The handbook retains 50 tables, 459 rows, and 1,167 cells, with 468-point widths and zero row minima. Evidence is in `.staging/weapons-migration-2026-09-19/redundant-lines-final-verification.json` and `redundant-lines-source-readback.json`; the source was modified at `2026-09-19T17:58:02.544Z`, with readback SHA-256 `33154344F135650A1BD00F929F8AAFB682A7B796791AA04597A414BF3759A2B6`.

`WeaponBases!A1` imports the ten canonical columns, including `techniqueKeys` and `traitsText`. `WeaponBases_Display!A1` produces one card per base with its rank, tags, description, traits, and the full technique blocks resolved by stable key from `_TechniqueBlocks!A:C`, separated by blank lines. The current 31 bases contain 32 technique relationships, including generic Covering Fire. These blocks reuse the same renderer and canonical mechanics as the general catalogue. Unknown references deliberately fail with a formula error so the formatter cannot publish an incomplete lookup. The six rank excerpts derive from that same card catalogue, preserving canonical source order; their row masks use `ARRAYFORMULA` so `FILTER` receives matching column dimensions. The existing bulk command refreshes the same `WeaponBases_Formatted` tab and six native links.

The embedded-card refinement is installed and verified at the following ranges. The six base-rank ranges retain their existing handbook links; N is an available excerpt, not a separate N-linked handbook table. Native recalculation, both individual formatter commands, main-catalogue resizing, and rich-text verification completed successfully. The main catalogue contains 108 records, the complete hidden pool contains all 139 canonical records, and 31 pool records have specific weapon owners.

| Formatted range | Content |
| --- | --- |
| `Techniques_Formatted!A1:A108` | Verified general catalogue, excluding 31 base-specific techniques |
| `Techniques_Formatted!L1:L14` | Surviving earlier weapon excerpt |
| `Techniques_Formatted!N1:N31` | Migrated weapon attacks and alternatives |
| `WeaponBases_Formatted!A1:A31` | All weapon bases, with 32 embedded full technique blocks |
| `WeaponBases_Formatted!C1:C4` | Rank 0 bases |
| `WeaponBases_Formatted!E1:E14` | Rank 1 bases |
| `WeaponBases_Formatted!G1:G4` | Rank 2 bases |
| `WeaponBases_Formatted!I1:I5` | Rank 3 bases |
| `WeaponBases_Formatted!K1` | Rank 4 base |
| `WeaponBases_Formatted!M1:M3` | Rank 5 bases |

Final compact-format checks verified all 32 technique references inside the 31 base cards, all 62 formatted weapon cells and 168 formatted technique cells against their native formulas, and all 32 `Function_Tests` checks passing, including the three new scaling checks. The shared pool retains 139 records; the main catalogue retains 108 after excluding the 31 weapon-specific records. Handbook readback matched all 153 entries—108 general techniques, 31 weapon cards, and 14 generic weapon-excerpt entries—in text and bold/italic styling. A native visual check confirmed the compact Longsword card is readable. At the preceding embedded-card checkpoint, five linked tables required **Match spreadsheet data and formatting** to remove Docs overrides; the compact-format follow-up preserves that parity.

The superseded 8-row, 6-column weapon-damage table was removed after its mechanics were preserved in the canonical technique records. The final compact-format audit reconfirmed 50 handbook tables, 459 rows, and 1,167 cells, with 468-point total widths and zero positive minimum row heights. Repository verification passed all 256 unit tests, all nine frozen-data baseline checks, and all 14 browser-asset entry checks. Runtime artifacts, publishing, and deployment remain unchanged. The preceding compact-format evidence is recorded in `.staging/weapons-migration-2026-09-19/compact-technique-final-verification.json`; its source checkpoint was modified at `2026-09-19T17:35:27.287Z` with SHA-256 `5A1762511E289DF0224A29413FD1161BCF7EDA028D8DA147E189774CD2DB275F`.

At the preceding names-only-card checkpoint, the native command completed all seven display pairs, creating `WeaponBases_Formatted` with sheet ID `1175877209` and retaining every existing output ID. Readback verified text and bold/italic runs for all 31 base catalogue cards and all 31 rank-excerpt cards; six native handbook tables linked those rank excerpts. The 23 migrated complete techniques and eight Rank 2 drafts remained intact, including their known roll details, explicit missing-mechanics labels, critical riders, and reload restrictions. All 24 `Function_Tests` checks passed at the 141-technique snapshot. Following the user's two deletions, that checkpoint's regeneration and catalogue resizing passed verification: all 139 formatted source entries matched all 139 handbook entries in text and every non-whitespace character's bold/italic styling, all 24 checks passed with 199 formatted and 199 calculated cells, and L14/N31 remained intact. Those historical counts do not verify the new filtered catalogue or embedded card layout.

Before the earlier two-row catalogue shrink, the handbook layout audit verified 51 tables, 500 rows, and 1,248 cells. That checkpoint's post-deletion audit verified 51 tables, 498 rows, and 1,246 cells, with 468-point total widths within floating-point tolerance and zero positive minimum row heights throughout. The guarded style-only merge preserved every table's text and emphasis and retained concurrent unrelated paragraph edits. The formatter tests also verify preservation of technique column N and sparse base-rank columns through M, reuse of output sheet IDs, full multiline blocks inside cards, exclusion of the hidden pool from formatting, and failure before output mutation when a source contains a formula error. All three focused formatter tests pass. Local tests do not evaluate Google Sheets named functions; the earlier native evidence is recorded in `.staging/weapons-migration-2026-09-19/final-verification.json`, and `docs/status.md` records the latest refinement verification.

After adding records, regenerate the formatted tabs and expand any native Docs link whose fixed range ends before the new rows. Existing Weapon Master excerpts are `Archetypes_Formatted!E1:E9` and `Feats_Formatted!E2:E8`. A range expansion can introduce Docs typography overrides; compare source rich-text runs, use **Match spreadsheet data and formatting** for the affected whole linked table when needed, and reapply the handbook's compact layout.

The earlier consolidation verification matched all 214 technique/catalogue/excerpt/feat entries against the handbook's text and bold/italic styling. At that checkpoint all 24 spreadsheet checks passed, all output sheet IDs remained unchanged, and the original 97 technique display entries were unchanged. Subsequent source edits already marked Disguise Makeup, Distant Whispers, and Watercolor Illusion as drafts with unassigned energy; the weapon display configuration now renders that current status accurately. Exporter compatibility remains explicitly deferred.

Earlier verification on 2026-09-19: the saved menu command completed the then-existing six display pairs, creating `ClassFeatures_Formatted` (hidden) and `Origins_Formatted` while retaining all existing destination IDs. Readback matched 502 populated output cells and confirmed unchanged source cells and all technique excerpt columns. The full unit suite passed 247 tests. That historical evidence is ignored under `.staging/display-batch-2026-09-19/`; weapon-migration evidence is under `.staging/weapons-migration-2026-09-19/`.
