# Current implementation status

Last updated: 2026-09-22

Current branch at update: `codex/work-package-b-data-contract`

Active implementation work package: Work Package E — vertical domain migration

Next implementation step: `WPE-DOMAIN-MIGRATION`

Current user corrections are implemented. Trait grants describe static acquisition and eligibility only. The September 22 continuation removes hand-state checks from static weapon eligibility, converts Celestial Knight's retired either/or choice to its surviving both-skills feature, and gives old Metamorph characters an explicit player-rebuild disposition while preserving their originals. The fresh candidate and local saved-character copies pass verification: 431 unit tests, 20 Firebase emulator tests, nine frozen artifacts and 14 asset checks. Exact data publishing and matching Hosting approval remain before the coordinated live release; the user's personal manual testing is nonblocking.

Parallel blocked character step: deployed-page completion of `WPC-REPOSITORY`, pending Work Package E integration and coordinated explicit-grant data acceptance; stable keys are available, but published data lacks grants required by the newer graph

Parallel game-data step: `WPB-SCHEMA-V5-INTEGRATION` is complete and `WPB-V5-RELEASE-REVIEW` is active. Schema-v3 publisher support, accepted source repairs and September 22 compatibility dispositions are implemented. Current candidate `20260922T184507461Z-43412` stages ten files with zero errors and 254 warnings. Removed identities and character impacts have explicit dispositions in the release review; the exact candidate and 128-file website package are prepared for separate publishing and Hosting approval. The published runtime release remains unchanged.

Trait implementation is complete for explicit character-owned providers: shared static Rules, schema-v6 choices, sequential migration, graph ownership and prerequisite evidence, portable Class/Origin controls, character-sheet display, tag-based eligibility and explicit granted-only Technique access. Provider `traitKeys` remains reference-only. Familiar/Mech recipients and unfinished individual mechanics remain held. Eight existing Origin providers and Metamorph's three choices now have supported native grants. Legacy schema-v6 activation values are preserved but ignored; this does not mark all of `WPE-DOMAIN-MIGRATION` or the broader coordinated release complete.

Current repair: `WPC-LIVE-MIGRATION-REPAIR` implementation and isolated verification are complete. The user's September 21 follow-up removes their deferred manual acceptance as a blocker; the verified Class-page hotfix is live as Hosting version `d5c9671adee0ab31`. Production character documents remain unchanged.

Manual acceptance policy for this work: the user said, "I'll perform manual acceptance soon, but not now. That should not be a blocker." Their personal acceptance is deferred follow-up, not a prerequisite for continued engineering or release of the verified Class hotfix. The earlier September 21 deployment scope was exactly the prepared one-file Class controller change, preserving the other 101 Hosting files and configuration. It did not authorize new game data, the full newer builder, Rules/Functions, or production character writes. Concrete compatibility failures and ordinary automated/browser verification still need resolution; do not describe the user's availability as the blocker.

This is the only frequently updated project-status document. Historical audits and completion records must not be edited to look current.

## Checkpoint

- September 22 prerequisite clarification: prerequisites primarily act as compile-time checks on the static character build, re-evaluated after relevant build edits. Gameplay conditions such as current Energy, available hands, position, triggers or active transformations do not remove learned choices or prevent builder saves. This is now recorded in the agent guide and living architecture/source/graph contracts, including correction of stale hand-state deferral text. Documentation-only continuation from clean `c764810`; preflight passes 431 unit tests and all nine frozen artifacts, and `git diff --check` passes. No application/data bytes or prepared release approvals change; the next boundary remains the coordinated `WPB-V5-RELEASE-REVIEW` publishing/Hosting approval above.
- September 22 continuation started clean at `accd4e3`. Preflight passed 424 unit tests, nine frozen artifacts and 14 asset checks. Read-only canonical Sheet metadata is unchanged at `2026-09-22T00:57:44.536Z`; no source cells or production character documents were edited. The user's three decisions supersede the pending questions in the earlier checkpoint below.
- Static weapon Rules now require matching owned equipment without consulting wielding or available hands. Counts, tags, key, reach, rank and enhancement candidate scope remain enforced. The fresh immutable run `20260922T184507461Z-43412` validates/runtime-loads ten artifacts with **0 errors and 254 warnings**, 48 fewer hand-state warnings. Authored source values and normalized model hash remain unchanged. Combined data SHA-256: `0cedfcc8359af9809360277017502b3cf5435cf733b4bcd143288879a1b6e396`.
- CharacterMigrations now includes a narrow, catalog-gated content operation independent of stored shape version. One known Celestial Knight child answer is absorbed only when its selected, unique surviving parent grants both weapon skills. V4/v5/v6 are covered, original choice/source provenance is reported, unknown or ambiguous alternatives are not inferred, and the next explicit save persists the conversion once. Old Metamorph receives `character-rebuild-required`; its original is preserved and no Traits are inferred. New Metamorph characters still work. The Class page no longer shows a false error when the user cancels a save proposal.
- Replaying the existing private 13-character capture against the fresh candidate gives **eight clean**, **one existing weapon-slot-capacity error**, **three requiring rule/content-change confirmation**, and **one old Metamorph requiring a rebuild**. Celestial Knight now decodes; comparing its two catalog decodes proves content migration changes only the obsolete answer. Its weapon rank/enhancement changes are separately reviewed. Hands no longer cause Technique removals. See [the release review](game-data-release-review-2026-09-21.md) for exact dispositions and evidence hashes.
- Full local browser verification uses copies of the captured Celestial Knight and Metamorph. Celestial Knight displays both skills and keeps its two feats. Cancellation leaves the original stored local copy unchanged; acceptance saves/reloads schema 6/revision 1 with the exact graph-reviewed state. Old Metamorph shows the rebuild instruction and remains unchanged. A direct emulator check also proves the new Metamorph review character retains its three Traits. Class console is clean; the copied Celestial sheet has only the expected unavailable-portrait warning because that production portrait is not present under the local owner.
- Final `npm run test:all` passes **431 unit tests, 20 Firebase emulator tests and 14 HTML asset checks**. The nine-file frozen baseline and `git diff --check` pass. Exact logs and private checks are ignored under `.staging/release-review/`. The local website was restarted at PID `54740`, `http://127.0.0.1:5015/`, serving current code plus the fresh candidate without writing production JSON. The prepared matching Hosting package contains 128 files; manifest SHA-256 `d955fdd1c22c9b0dc05107f84f308efc77da37e20debcd98800ec7263ea36256`. Its draft approval flags remain false. No unrelated pre-existing worktree changes were present.
- Next boundary remains `WPB-V5-RELEASE-REVIEW`: obtain exact candidate publishing and matching Hosting approval, then promote the coordinated data/application release and verify the live result. No Rules/Functions or manual production character edits are in that scope. This continuation completes the three requested corrections, not every deferred subsystem or the entire Work Package E. Personal manual acceptance remains follow-up; the prior Class-only hotfix remains the sole production deployment.

Earlier September 21 continuation checkpoint:

- `WPE-DOMAIN-MIGRATION` / `WPB-V5-RELEASE-REVIEW` continuation started clean at `240b5a0`; preflight passed 419 unit tests, nine frozen artifacts and 14 asset checks. Canonical Sheet metadata remains unchanged from candidate `20260922T005923417Z-35172`. The [release review](game-data-release-review-2026-09-21.md) records every removed identity group, the exact candidate, private-evidence hashes, proposed dispositions and unresolved decisions. This turn edited no source cells or production records.
- Fresh read-only production capture at `2026-09-22T01:57:33.763Z` contains 13 current-path characters plus one legacy-path document. After the repairs, eight current-path records decode/reconcile without errors or destructive changes, one has the existing six-of-four weapon-slot error, two have rule/content-change confirmations, and two cannot resolve retired Celestial Knight/Metamorph references. These findings are not permission to discard saved answers. The reported Spirit Warrior's feat is retained by the candidate's explicit grant. Private evidence is ignored under `.staging/release-review/`.
- Skill Rules now treat named grants as free floors, including old extra rows repeating granted skills, case differences and the Targeting alias. Canonical user-owned rows survive removal of their grant and receive reviewed budget adjustments only if necessary. The character sheet consumes the same Rules display projection, resolving utility keys to the correct labels/core fields and preserving higher paid ranks. Incomplete option-group choices are visibly unavailable before clicking; Origin proposal completion restores focus after controls are enabled.
- Full signed-in local browser verification traversed all eight builder pages with the exact candidate: Metamorph and three owned Trait choices, Attributes, Origin, free/paid Skills, ordinary equipment, an intentionally empty Technique selection, a Bond and Keystones saved/reloaded. Mutation's automatic Traits and all three class choices appear on the sheet with their source/rank/tags. Cancellation preserves the previous Origin and Traits; acceptance saves/reloads the reviewed change while retaining class choices. Both cancelled and accepted Origin proposals restore keyboard focus. Two-tab stale saving is rejected with a visible conflict and preserves the first tab's text. Final direct emulator read verifies schema 6, revision 14, all eight visited steps and retained answers; the review character was restored to Mutation. Browser console has zero warnings/errors.
- Final `npm run test:all` passes **424 unit tests, 18 Firebase emulator rule tests and 14 HTML asset checks**; all nine frozen artifacts and `git diff --check` pass. Five added regression tests cover granted-rank charging, paid overlays/source removal, utility key placement, aliases and unknown-rank preservation. Logs and local character checks are under `.staging/release-review/`. The complete candidate website was restarted at PID `48900`, `http://127.0.0.1:5015/`, with current-code HTTP/browser verification; Firebase emulators and existing local data are preserved. This preview overlays the exact staged artifacts without writing production JSON. No unrelated pre-existing changes were present.
- The two active steps remain open. User answers are pending on static weapon-use eligibility and preserving retired Metamorph choices for explicit replacement. Celestial Knight's obsolete either/or answer also needs an evidence-backed compatibility conversion to the surviving both-skills feature. Metamorph's prose-only Natural Weapon/Technique rules and generic Keystone execution remain separate implementation work. Resolve these concrete compatibility questions and prepare exact release bytes before the separate publishing and broader Hosting approval; personal manual acceptance remains nonblocking. Production still has only the earlier Class hotfix.

- September 21 static Trait implementation: immutable shared Rules project automatic/choice providers, fixed or associated-skill rank, explicit acquired tags, ready Technique access and prerequisite closure without circular self-authorization. Grants support named keys or tag-filtered choices, default count/rank 1 and optional associated-skill scaling. Source ownership derives from the granting feature. Codec, typed commands, graph reconciliation and revision-aware persistence agree on choice ownership; schema-v3 Origin nodes use stable feature keys. Legacy `traitActivations` remains inert compatibility data and is not deleted or used to gate benefits. No new character schema is needed for this correction.
- Portable Trait widgets are integrated on Class and Origin with distinct category labels and no activation controls; the character sheet shows the same projection read-only. Explicit granted-only associated Techniques are free, while tag-routed Techniques use normal learned capacity. Classification categories remain filters. Provider descriptions retain costs, ranges, durations and form conditions for players to track. Web Shooters explicitly supplies Web; Wings supplies Wings at Rank 1 and Flight at Rank 2. Incomplete linked Techniques stay unavailable without disabling the Trait or whole class.
- Verification passes **419 unit tests, 18 Firebase emulator rule tests, and 14 asset checks**. Fixtures cover all eight real Origin providers, grammar rejection of gameplay/recipient fields, tag eligibility versus free acquisition, source ownership, inert legacy activation data and reviewed removal/cancellation. Five browser scenarios use the actual TraitWidget, CharacterSessionPage and graph, including fresh staged Metamorph: choice/focus, dependent-removal cancellation, accepted source removal, three distinct Metamorph selectors, and exact schema-v6 local save/full reload. Web Shooters, Wings and Climber retain their owned answers, rank, tags and revision. Console has no warnings/errors. Separate authenticated emulator tests cover migration, first explicit save, exact Trait state and stale revisions. Evidence is ignored under `.staging/traits-integration/`; no browser production-character save is claimed.
- Local Firebase was restarted with data preserved at `http://127.0.0.1:5000` (UI port 4000). The static Trait preview with the fresh staged data is `http://127.0.0.1:5014/static-browser-review.html`; the earlier activation preview on 5013 is obsolete. All nine frozen production artifacts still match the baseline. No production character write, new data publish, or broader website deployment occurred. The user's deferred personal acceptance remains nonblocking.
- The accepted [27-cell source changes](trait-source-proposals-2026-09-21.md) repair three ClassFeatures references/counts, formalize eight Origin providers and three Metamorph choices, add the Web tag/Technique eligibility route and update contract guidance. Exact prewrite/readback checks cover 357 cells and preserve neighbors, formatting, validation, notes and rich text. Fresh immutable run `20260922T005923417Z-35172` has **0 errors, 302 warnings**, ten artifacts and successful runtime loading. Source modified time is `2026-09-22T00:57:44.536Z` (September 21 local time), Drive version null, and XLSX SHA-256 `6968f1d21d6615937ee48b04831febba645058975a9f5df17bf4244c14d51f00`. Metamorph is selectable and its Trait choices execute. Incomplete feats/Techniques remain incomplete; remaining prose-only Metamorph Technique grants and generic Keystones are not implemented by this correction.
- The display spreadsheet's headers are unchanged and its generic grant fallback retains Trait expressions. A dedicated readable Trait grant renderer and display/handbook refresh remain follow-up work; no such live refresh is claimed here. Broader release compatibility and the exact candidate approval boundary remain open, rather than waiting for personal manual testing.
- Publisher support now accepts exact source-v4/runtime-v2 nine-file or source-v5/runtime-v3 ten-file releases, including Traits. Hash/approval/provenance and version coherence checks remain mandatory; rollback tests cover upgrade/downgrade installation failures. Cleanup failures after a committed installation warn without deleting the new release. The old approved candidate still verifies. `WPB-V5-RELEASE-REVIEW` must still disposition removed stable identities and saved-character impacts, then obtain exact candidate approval before publishing; subsequent compatible application deployment remains separate. This is an engineering/data boundary, not a wait for the user's personal testing.

- Trait implementation preflight on September 21 passes 361 unit tests, nine frozen-artifact checks, and 14 asset checks. A fresh read-only native export is stable at source modified time `2026-09-21T18:54:27.168Z`, SHA-256 `ecb9b9a23702b83bc0086d2d5be03cf94592f436b6cbf59012e892250380d2d6`. This supersedes the earlier candidate for current-source claims. Run `20260921T224023044Z-46084` stops with six errors: three newly authored `trait` grants need importer support, `ClassFeatures!H238` has no option count, and `I241:I242` reference missing Techniques. Shared Trait implementation can proceed against fixtures; this source snapshot cannot be published. Preserve the unfinished Metamorph design and prepare concrete source corrections separately rather than bypassing validation.

- September 21 live-save repair: read-only production inspection found 13 per-user schema-v4 characters and one older single-character document. Migration originally accepted four of the 13; the repaired registry accepts all 13 and preserves their complete canonical state through authenticated emulator save/reload. Reads never write, the first explicit save upgrades once, later reads do not rerun migration, and stale revisions are rejected. Unknown/ambiguous references still fail. Private evidence stays ignored under `.staging/firestore-character-audit/`; tracked fixtures are synthetic. No production character was written.
- The reported Class-page fault is separately reproduced from the deployed application: an unimported feat lookup throws before Firestore receives the save. An import-only experiment exposed further loss of feat ability text and sheet-owned HP/notes. The final bounded repair uses compatible feat/level lookup and sanitized skill/ability leaf writes. Browser save/reload succeeds, preserving the selected feat/ability, schema v4, and newer HP, strain, notes, and conditions changed while the page was open. [The investigation](live-save-repair-2026-09-21.md) records the evidence.
- The user's override released the Class-only hotfix at `2026-09-21T22:27:06.868Z`, replacing Hosting version `ff9730b38f571322` with `d5c9671adee0ab31`. The authoritative 102-file manifest verifies exactly one changed path, `js/builder/builder-class.js`; the other 101 hashes, all nine production data artifacts, and actual serving configuration are preserved. Public HTTP readback matches the tested controller SHA-256 `732c8f456b6574eec5bcecb7ab1ec27f668187cae26272bd82e1a957c83bb283`, and all nine public data files match the frozen baseline. The full newer builder/migrator, new game data, Rules, Functions, and production character documents were not deployed or modified. Deployment evidence is in ignored `hosting-hotfix-deployment.json`; the previous Hosting version remains the rollback target.
- Final repair verification passes **361 unit tests, 17 Firebase emulator rule tests, and 14 HTML asset checks**. All **13/13** captured current-path records pass the isolated persistence rehearsal; all nine production artifacts match the baseline; `git diff --check` passes. Local Firebase was restarted with data preserved at `http://127.0.0.1:5000` (UI port 4000). Preflight passed 348 unit tests from clean commit `e0fd417`; no unrelated user edits were present.
- Broader release findings remain: two records have equipment reconciliation errors and seven produce confirmation-required impacts. The reported character's new-builder Save preview would remove a valid feat because published schema-2 data lacks an explicit feat grant; the preview was cancelled. The staged schema-3 candidate supplies that grant and assigns the same feat correctly. `WPE-DOMAIN-MIGRATION` needs signed-in acceptance against compatible reviewed data, coordinated with `WPB-V5-RELEASE-REVIEW`. Format migration does not authorize applying these removals, inventing equipment training, or completing unfinished classes.

Prior September 21 importer checkpoint:

- `WPB-SCHEMA-V5-INTEGRATION` now imports source schema v5/syntax v3 into ten runtime schema-v3 artifacts. All three Classes skill roles remain authoritative, yielding 83 distinct relationships including Weapon Master's conditional progressions. Technique access alternatives, associated skills, generic pumping, underlying basic attacks, Traits/providers, typed prerequisite alternatives, known-option counts, explicit recipients, and fresh feature invocations survive normalization. The v4 path retains its exact schema-v2 fixture bytes.
- Runtime Rules and graph/widget integration honor authored readiness and explicit execution deferrals. Feat category/type alternatives remain restricted, class-granted skills satisfy the same prerequisites in graph and widgets, and enhancement prerequisites inspect the candidate weapon. Weapon rank caps/cards derive from stable Technique references. Generic Keystone choices, conditional evolution, recipient-owned skills, broad Trait/provider systems, and unavailable equipment-hand state remain deferred; no character persistence fields or missing game mechanics were invented. The known Integrated Weapon identity change has an explicit alias; unrelated removed identities are not guessed from names.
- Final immutable run `20260921T183411964Z-31844` has **0 validation errors, 300 warnings**, successful runtime loading, and a complete semantic diff: one added Traits file and nine changed files. All **951 source records / 13,901 authored cells** match retained source evidence, including blank mechanics and the clarified duration. Two independent runs produce identical hashes for all ten artifacts. The export retains source modified time `2026-09-21T18:05:43.676Z`, explicit null Drive version, and XLSX SHA-256 `28c7ec9c1e6cb0bea54b34200c9c2a1bc5be27df47fa84b51c5499823ce9bdc4`; provenance, reports, diff, and artifacts are under ignored `.staging/game-data/runs/20260921T183411964Z-31844/`, with preservation evidence under `.staging/schema-v5-integration/`.
- `npm run test:all` passes **348 unit tests, 16 Firebase emulator rule tests, and 14 HTML asset checks**. `npm run baseline:data` verifies all nine unchanged production artifacts; `git diff --check` passes. The local Firebase review environment was restarted with existing data preserved at `http://127.0.0.1:5000` (UI `http://127.0.0.1:4000`); HTTP readback verifies the current modules. No production publish, remote deployment, or signed-in acceptance is claimed. The initial importer worktree was clean at `975a43b`; no unrelated user edits were included.
- Release review must disposition the diff's **11 removed Technique keys, eight removed Feat keys, four removed ClassFeature keys, and 33 retired weapon profiles** before promotion. These source deletions are separate from format compatibility. The publisher deliberately still requires the existing nine-file schema-v2 contract. `WPB-V5-RELEASE-REVIEW` makes compatibility and candidate approval concrete; parallel `WPE-DOMAIN-MIGRATION` still requires signed-in save/reload, destructive confirmation/cancellation, and conflict/focus acceptance before application release.
- `WPB-SCHEMA-V5-INTEGRATION` preflight passes 300 unit tests, nine frozen artifacts, and 14 HTML asset checks. Command-line source access currently fails with insufficient read-only Drive access. A fresh authorized connector export supplies an independent native XLSX snapshot for implementation checks: 150 Techniques, 60 Traits, and the resolved Eyes of Legend duration, with unchanged modification time across export and SHA-256 provenance under `.staging/schema-v5-integration/`. This does not claim that command-line authentication has been restored.
- September 21 duration follow-up: Eyes of Legend lasts until the end of the user's current turn. The authorized change updates only `OriginFeatures!F7`; exact native readback across A1:L10 confirms all neighboring values, formatting, validation, notes, and rich text are preserved. Evidence is in ignored `.staging/schema-v5-integration/eyes-duration.json`. This resolves Q36 before importer implementation.

Prior September 21 content-decision checkpoint, before importer integration:

- September 21 content decisions are applied to the canonical source: 270 cell changes, optional `Techniques.basicAttack`, and retirement of `grantText` from ClassFeatures, Feats, and OriginFeatures after preserving 16 meaningful descriptions. Thirty-one basic-attack wrappers now identify their underlying attack and any explicit override; wrappers with no additional roll use `rollRequired=N`. Monster Evolution has one canonical conditional rule and four later feature references, each owning a fresh application. Keystone uses the generic choice form; Living Archive's three Rank 1 skills explicitly belong to its Artifact. The user's answers and remaining omissions are recorded in [the content decisions](source-content-review-2026-09-21.md).
- The current prose governs repairs to stale copied grants; old assistant notes do not override authored mechanics. The three Classes skill fields, strain costs, critical-failure outcomes, and existing readiness remain. Broader Metamorph, Monster Tamer, Mech Pilot, and other unfinished class design remains held. Incomplete content is valid migration input; preserving it does not authorize execution of unsupported rules or invent missing mechanics. The handbook supplied Eyes of Legend's 12-square range; the user subsequently specified the end of the current turn as its duration.
- Independent replay of the first 265 changes verified 31,499 cells / 157,495 native property slots with zero unexpected differences. A separate delta check verified all five follow-up corrections, 835 explicit references, Schema headers/types, and key/parent relationships. Fourteen concurrent Trait-tag edits and later user changes to Apex feats, Integrated Weapon, and Crushing Limbs were preserved. Integrated Weapon's user-authored key change needs identity compatibility handling during integration. The verified source snapshot was modified at `2026-09-21T17:45:08.156Z` (Drive version unavailable); CellData JSON SHA-256 is `8f36d3a92e31e46783726d2b758b9d6982880421de7543d0b3730cccd9a1aabe`. Plans, recovery snapshots, native readbacks, independent reports, and provenance are ignored under `.staging/content-decisions-2026-09-21/`; this is an authoring readback, not a staged runtime release.
- Formal grant displays resolve labels by stable key and retain recipient ownership. Technique display supports underlying attacks, conditional free Reactions, and typed prerequisite alternatives separately from value-level alternatives. All 87 native formula checks passed before later concurrent authoring. The final native check has 85 passes and two coverage-count differences: formatted Techniques 176 versus 177 current outputs, and formatted Traits 57 versus 59 current outputs. These reflect subsequent live additions and require the ordinary display/book refresh after editing; there are no native formula errors. The implementation fixtures still pass.
- Display and handbook parity passes for the captured snapshot: all 701 formatted cells / 237,231 non-whitespace characters match their eight raw output tabs, and all 36 managed handbook tables / 460 entries match their formatted source, including 152,119 styled characters. All 1,480 unrelated prose paragraphs and 13 manual tables preserve content and styles except two approved mechanical corrections: full-rank High-Impact Ammunition push and Living Archive skills at Rank 1. All 49 tables / 550 rows / 1,258 cells retain expected table formatting. The final handbook refresh includes the captured Integrated Weapon and Crushing Limbs edits; subsequent source additions are outside this snapshot. Native text/style verification passed; no screenshot-based acceptance is claimed.
- Repository verification passes 300 unit tests, nine frozen artifact checks, and 14 HTML asset checks. The initial worktree was clean at `cca3f49`. This work updates source/display authoring and its contracts; runtime acquisition/import, production data, and website deployment remain unchanged. Next game-data step is `WPB-SCHEMA-V5-INTEGRATION`: support the accepted format and source-owned relationships, preserve incomplete records with precise diagnostics, maintain saved identities, and produce a staged diff. Publishing remains a separate review boundary; the parallel `WPE-DOMAIN-MIGRATION` signed-in acceptance boundary is unchanged.

Prior September 21 audit checkpoint, before the user's decisions:

- September 21 content audit: the user authorized clear inconsistency repairs and requested a clarification list afterward. Reviewed all 13 source tabs and 944 entity records. Applied 221 cell corrections across 144 rows in ten tabs: copied grants, explicit missing grants, malformed prerequisites, choice ownership/counts, misplaced mechanics/editorial text, stale cost/readiness metadata, and spelling/grammar. The three distinct Classes skill fields, Technique strain cost, critical-failure outcomes, and unique existing identities remain intact. Only Transformation Keystone's duplicated feature key was repaired; the original Metamorphosis Techniques key remains unchanged. The user's two Chimaera identities now exist, superseding the earlier missing-identity finding.
- Exact request replay and an independent check agree across 30,759 cells and 153,795 native cell properties, with no unexpected differences. Rich-text boundaries were rebased in four edited feat descriptions. All primary keys are unique and all option parents/counts are valid. The audit matched all 117 Schema fields, checked 1,059 numeric cells and 840 formal references. Remaining issues, including Keystone grant syntax and Mixed Stance's invalid resource prerequisite, are explicit in [the 39-item content review](source-content-review-2026-09-21.md); missing recipient/activation syntax is separated from game-design decisions.
- The dependent display check exposed omitted ClassFeatures prerequisites. Four existing named helpers now render formal requirements for feature/group/option records by stable key; Class/Origin projections tolerate an absent optional prerequisite header. Nine duplicated fighting-style prerequisite prefixes are removed from source without losing their printed requirements. All 67 native checks pass, including eight new regression fixtures, and all eight formatted tabs have been regenerated. Source tabs were visually inspected at normal zoom; the hidden display browser surface still reports zero width, so display/handbook visual acceptance relies on native text/style comparison.
- Final display/handbook comparison passes: all 697 formatted cells / 241,051 non-whitespace characters match the eight raw outputs; all 36 managed handbook tables / 456 entries match their formatted source, including 154,109 styled characters. All 1,480 unrelated prose paragraphs and 13 manual tables are unchanged. The 49 handbook tables retain their expected widths, zero row minima, borders, striping, and alignment. Source modification time is `2026-09-21T16:07:43.423Z` (Drive version unavailable); captured CellData JSON SHA-256 is `5a399365dfc0df4198acc357ff51353f5fb000ba6c34a2cc8aed4b180f0ad905`. Recovery/readback, exact plan, independent verification, final display/handbook comparisons, and provenance are ignored under `.staging/content-audit-2026-09-21/`. This is an authoring readback, not a staged runtime release.
- Repository verification passes 295 unit tests, nine frozen artifact checks, and 14 HTML asset checks. The initial worktree was clean at `ba67285`. This change is source/display authoring and documentation; runtime importer, production data, and deployment remain unchanged. Next is resolving the content-review questions, then `WPB-SCHEMA-V5-INTEGRATION` to acquire, normalize, and validate the simplified source with faithful ownership and eligibility. Its staged output still requires separate publishing review; `WPE-DOMAIN-MIGRATION` signed-in acceptance remains the parallel application boundary.

- September 21 `WPB-AUTHORING-V5` is complete under the user's approval of the source cleanup and explicit corrections. The 147 Techniques now use 30 authored columns: selection, readiness status, optional associated skill, formal prerequisites, and generic pumping. Redundant damage/provenance/notes/prerequisite-text/skill-key/tag-key fields are retired after preserving their meaningful content. All three distinct Classes skill columns remain authoritative; all 78 class-skill relationships, including Weapon Master's conditional progressions, were proved equivalent before retiring ClassSkills. The empty WeaponProfiles tab and six unused/redundant columns elsewhere are retired. Strain cost and critical-failure outcomes remain. Traits has 55 records and explicit recipient-tag grants separate from classification tags.
- Source verification replayed the exact requests against recovery snapshots: 22,091 captured cell values and 88,364 native format/validation/note/rich-text properties matched with zero unexpected changes. All 147 Technique identities and all 36 pumping maps survive; readiness remains 109 playable, 30 draft, and eight incomplete. README, Metadata, Schema, Enums, header guidance, and status validation match schema v5/syntax v3. Final metadata confirms 13 source tabs. The two incomplete Chimaera feat rows (77–78, missing key/name) remain visible in display diagnostics; they do not break other cards. Unresolved Beam requirements retain their authored text, and unknown mechanics were not invented.
- Display adapters retain compatibility positions while missing optional source headers produce correctly sized blank fields. Technique blocks, feat blocks, and option-parent relationships resolve directly by stable key, including duplicate display names. Thirteen native named helpers and their callers are updated. All 43 existing native checks plus 16 new regression fixtures pass. The formatter regenerated existing output tabs; 653 cells and 222,922 characters match raw Markdown across all seven display/formatted pairs.
- The existing automatic handbook refresh completed. All 36 managed tables / 454 populated rows match their formatted source text and rich-text styles, including 153,833 non-whitespace characters checked. All 1,480 unrelated prose paragraphs and 13 manual tables preserve content and styles. All 49 tables retain the expected 468-point layout, borders, stripes, and alignment. Browser accessibility/readback verification succeeded, but screenshot capture could not run because the browser surface reported zero width; no screenshot-based visual acceptance is claimed.
- Final authoring readback records canonical Drive ID `1TEdxuufglP8lFRNk8QD4N_351-0ihAUFLG2743ESjoI`, modification time `2026-09-21T15:33:26.399Z` (version unavailable), and normalized snapshot SHA-256 `d2b2978fa47db6921b40053e8e08407107d0f5b29c9bdfc8191513e0869d3fee`. Recovery snapshots, exact source plans, provenance, native regression results, and independent source/display/handbook reports are ignored under `.staging/schema-cleanup-2026-09-21/`. This authoring JSON readback is not a runtime acquisition or staged release candidate.
- Verification passes 292 unit tests, nine frozen production-artifact checks, 14 HTML asset checks, and `git diff --check`. The initial worktree was clean at ef3a91d; changes in this feature are authoring tooling, display formulas, tests, and living contracts. Runtime application files, source-acquisition version gates, production data, and deployment are unchanged. The next game-data step, `WPB-SCHEMA-V5-INTEGRATION`, will implement acquisition/adaptation/validation and runtime support for the accepted source, preserve saved-state identities, and produce a staged report/diff. Missing source identities and unfinished mechanics must remain explicit findings; publishing and deployment require their separate review boundaries. The parallel application boundary remains `WPE-DOMAIN-MIGRATION` signed-in acceptance.

- The Trait consolidation repair is verified for the 54-Trait source snapshot. The eight core columns render without the removed editorial metadata; optional fields default to correctly sized blank arrays, while missing required references still fail. All surviving Origin Trait references resolve, Web Shooters links to Shoot Web, and nine associated/tag-eligible Techniques now start at their lowest enabling Trait rank. Schema guidance and handbook prose record provider-defined associated skills, skill-based Trait rank, independent Familiar-rank scaling, and Origin benefits that need no skill. Quadrupedal is retired and Slime Physiology points to Inorganic Nature.
- Both display refreshes preserved the user's concurrent work: the new Metamorph feature chooses three Traits by tag and uses Metamorphosis, the specific form rows remain deleted, the revised Slimefolk handbook paragraph remains intact, and the unrelated Doppelganger skill edit remains intact. Source comparison verified 495 Trait cells, 5,215 Technique cells, 336 Origin cells, and 84 Trait-schema cells; formatting, rich-text runs, notes, and validation are unchanged. The repair accounts for one Trait reference cell, 16 Technique cells, 14 Origin cells, and eight schema cells; the earlier class edits were superseded by the user's new feature.
- Native renderer fixtures pass for omitted metadata, present metadata, and a dangling Technique reference. The rank check passes for all eight direct Trait-to-Technique links; Venom Spray also has Rank 1 through its Poisonous prerequisite. All 43 native checks passed after the first refresh. The final readback keeps all Trait checks passing; two unrelated coverage checks drifted during further concurrent editing (formatted Technique cells 177 versus 176 current, and archetype memberships 60 versus 61 current). These need the ordinary next display/book refresh once the user finishes editing; they are not broken Trait lookups.
- Handbook readback matches all 54 Trait cards and 117 general Technique cards to the reviewed formatted snapshot in text and bold/italic emphasis. The repaired Origin paragraphs, current Metamorph feature, shared scaling rules, and updated Mech index are present; native visual review passed. The final source readback records modification time `2026-09-20T20:20:08.568Z` (version unavailable), SHA-256 `DDC8CDA28F5B57DA210C3DDEBD435348DB1F488B46476111FEB5F7978C920F7C`, and authoring evidence under `.staging/traits-repair-2026-09-20/`. This is not a runtime acquisition or release candidate.
- Repository verification passes 265 unit tests, all nine frozen-artifact checks, all 14 HTML asset checks, and `git diff --check`. Application/runtime integration and publishing remain deferred. The next application boundary remains `WPE-DOMAIN-MIGRATION` signed-in acceptance, which verifies editing and saving through the shared session/graph; it does not authorize production deployment. Remaining Trait authoring decisions include unfinished mechanics and provider-specific ranges such as Eyes of Legend.

- The September 20 follow-up converted 36 registered handbook source sections to user-approved script-managed tables. All 476 populated rows match the display spreadsheet's text and rich-text styles, including 158,694 non-whitespace characters checked for emphasis, font, size, color, and links. Henshin archetypes now contain nine populated entries instead of a 100-row padded range; growing feat/archetype sections include their new rows. All 49 handbook tables have the intended 468-point width, stripes, borders, compact padding, and zero minimum row heights. Native Henshin and Psychic checks confirm left table alignment and top cell alignment. Unrelated prose and manually authored table text are unchanged. Automatic-open and **Handbook → Refresh tables** executions completed in approximately 115 and 124 seconds. A final readback proves table content and styles unchanged across all four tabs after these repeat runs; an overlapping open run exited safely under the lock.
- The source reader now uses the read-only Sheets advanced API because `SpreadsheetApp.openById` requires spreadsheet-edit permission. The explicit permission set is Docs, read-only Sheets, and trigger management; live preview, editor refresh, automatic-open refresh, and manual menu refresh succeeded without another consent prompt. The formatter re-reads and rebuilds its indexed plan after revision conflicts. Tests cover 105-row growth, blank filtering, source typography, Docs carriage-return normalization, permission scope, anchor safeguards, and three-attempt revision retry limits. All 15 focused tests, 265 repository unit tests, nine frozen artifact checks, 14 HTML asset checks, and `git diff --check` pass. Recovery, native UI, and zero-failure readback evidence are saved under `.staging/handbook-managed-2026-09-20/`. Runtime data and application release boundaries are unchanged.

- The handbook formatter is active as of September 20. The missing Docs advanced service was enabled after explicit terms approval; installer errors from calling the document UI in the script editor and comparing internal trigger IDs with Drive IDs were fixed. Setup retains one document-open formatter trigger and preserves unrelated triggers. Both automatic-open and manual menu executions completed successfully for all 50 observed tables. Source cells, runtime artifacts, and the Work Package E release boundaries are unchanged.

- The September 20 Trait authoring migration uses the approved compact format: name and rank together, separate Prerequisites and Tags lines, ordinary benefit prose, then `Rank N+` scaling. Canonical `Traits` contains 77 separate records: 25 existing shared/Familiar Traits, 25 ranked Mech upgrades, 19 Metamorph adaptations, and eight selected physical Origin capabilities. Sixteen possible-duplicate families remain marked and unmerged; unknown ranks/tags and incomplete mechanics were not invented. Broader class-feature conversions and Mutation redesign remain outside the approved scope.
- Fourteen associated Techniques were added to the 137-record fresh source snapshot, producing 151 records and a 120-entry general catalogue. All 4,795 cells across the original Techniques records are unchanged. The 11 approved ClassFeatures/OriginFeatures providers now reference Traits; source QA compared 8,036 cells and found exactly 24 planned changes (11 descriptions, 11 `traitKeys` lists, and two headers), with 8,012 cells unchanged. Both activation/selection wrappers and 1,508 captured formatting/validation/note/rich-text properties remain unchanged. Eyes of Legend grants Spirit Sight; the three Mutation adaptations, Getaba Keen Senses, and three Slimefolk body features retain their existing mechanics through separate Trait records.
- Trait source/display/Handbook verification passed for all 77 Trait cards and all 120 general Techniques in visible text and bold/italic styling; Docs only normalizes trailing spaces in 31 Technique cells. Both native tables remain linked to their formatted source, are 468 points wide, and have zero minimum row heights. The Monster Manual chip, activation rules, all 22 unranked Mech brainstorming lines, and unrelated prose remain intact. Both affected tables of contents are refreshed, and native visual/link review passed. Evidence includes `doc-final-qa.json` and `handbook-native-final.json` under the ignored migration directory.
- All 39 native display checks pass, including the new Trait, column-order, and missing-mechanic checks. The Technique compatibility import normalizes all 35 fields by header name, preserving the source's reordered columns; blank-field checks distinguish missing values from authored numeric zero. A conceptual review removed an invented exclusive Elemental Blaster prerequisite from generic Elemental Blast; both existing providers remain possible. Concurrent source feat additions were preserved and their formatted output regenerated; these counts are checkpoint observations rather than fixed future totals.
- Final source readback records Drive modification time `2026-09-20T16:10:04.952Z` (version unavailable through the connector) and SHA-256 `4BCE28FA092C60B2EF133A530C93D1DB023AEB42B4F1AB18ECB4AAAE2CBF1189`. All 77 Trait records and 14 new Techniques match the corrected migration plan; all 4,795 original Technique field values remain unchanged after normalizing the concurrent column reordering. This is an authoring JSON readback, not a runtime acquisition or staged release candidate; evidence is ignored under `.staging/traits-migration-2026-09-20/`.
- Final repository verification passed 257 unit tests, all nine frozen artifact checks, all 14 HTML asset checks, and `git diff --check`. The concurrent handbook formatter work was preserved in its separate `8276e6f` commit. No application/runtime file or deployment changed. The next application step remains `WPE-DOMAIN-MIGRATION` signed-in acceptance; exporter integration and reviewed data publishing remain separate boundaries. For Traits authoring, the next decisions are resolving the marked duplicate families and missing ranks/mechanics; broader feature-family conversions still need their own approved scope.

- The wording cleanup is verified across source, display, and handbook. Repeated base availability, technique provider/availability (including draft intended availability), and zero-Energy lines are absent. Exactly 23 basic-attack `rankNotes` cells changed: 19 are blank and four retain the Rank 0 no-pumping restriction; all other source cells across 139 techniques are unchanged. All 13 remaining nonempty rank notes render intact, alongside skill Access lines, `Incomplete technique`, and missing-mechanic notices. All 32 native display checks pass, and all 153 linked handbook entries match current text and bold/italic styling. Standard preflight passed 256 unit tests, nine frozen artifact checks, and 14 HTML asset checks.
- The preceding September 19 compact-format correction was verified across source, display, and handbook. Exactly 69 cells across 23 weapon basics express linear damage growth once, leave `damageByRank` blank, and initially retained concise Energy/Rank 0 notes. Every old damage-map value was proved equal before clearing; pumping maps and all other source cells across the 139 observed technique records were unchanged. Shared rendering groups adjacent equal pumping coefficients with their original units and preserves distinct higher-rank benefits and minimum Energy costs. All 32 native display checks passed, and all 153 linked handbook entries matched text and bold/italic styling at that checkpoint.
- The September 19 weapon-card refinement is verified. All 23 basic attacks now preserve their starting damage and grow automatically with weapon rank (+3 for 13 standard attacks, +2 for eight weaker attacks, +4 for Greatsword/Warhammer). Their base attack costs 0 Energy with optional pumping, using the preserved Rank 0/1–2/3–4/5–6 pumping schedule. Alternative costs and unresolved Rank 2 actions remain unchanged; those drafts are still incomplete.
- The approved September 19 weapon migration replaces 19 basic attacks and four alternatives with uniquely named, weapon-provided Techniques, folds nine critical effects into critical-success riders, and preserves Iaijutsu as a Katana trait. Eight incomplete Rank 2 techniques retain the handbook's stated mechanics without inventing missing costs/actions. WeaponBases now owns stable technique references and traits; the 33 obsolete profile rows were cleared only after replacement readback. Their hidden tab/header remain for acquisition compatibility.
- The user confirmed automatic access while wielding the weapon, without spending learned technique choices. The Ranged Weapons name/key is applied to authored skill references, display formulas, handbook text, and application projections. Compatibility preserves old saved ranks and source-owned choice identities. The nine frozen production artifacts are unchanged; automatic weapon-technique runtime integration and data publication remain deferred.
- Concurrent source and handbook edits were preserved. The initial 110 techniques excluded the user's deleted `brutal-strength` and `bullet-spray`; after this migration produced 141, the user also removed `snap-kick` and `crooked-cobra`, leaving 139. Feat/class-feature/skill rows continued to grow during the work; current counts are observations, not migration assumptions. The requested rename was applied narrowly to eight additional skill references found in the later ClassFeatures read.
- Weapon bases now embed full technique blocks in their six linked handbook rank tables. The shared `_TechniqueBlocks` pool contains all 139 observed source techniques, with 31 base-specific actions excluded from the 108-entry main alphabetical catalogue. All 31 base cards resolve their 32 references from that pool; generic Covering Fire stays in the main catalogue as well as Machine Gun's card. The existing 14-entry generic weapon excerpt is preserved. The obsolete Weapon Damage table was removed after verification, retaining its heading and replacement explanatory paragraph.
- The final wording-cleanup audit verified all 50 tables at 468-point width, with zero minimum heights across 459 rows and 1,167 cells in total. Three native formatting repairs and 34 layout requests were scoped to three changed weapon tables; native visual review confirmed Longsword remains readable with the three redundant lines removed. These follow-ups change source/display/handbook authoring only; application code, runtime JSON, and deployment are unchanged. `WPE-DOMAIN-MIGRATION` focused browser acceptance and production release boundaries remain unchanged.

- `master` ends at `42bfb99` (`Update Architecture to be more Module`).
- Milestone 0 stabilization is committed at `7d4fc6e`.
- The initial Work Package B baseline is committed at `637c06f`.
- `WPB-SOURCE-SYNC` is the checkpoint commit immediately after `637c06f`, titled `Document handoff architecture and automate source acquisition`.
- The current Work Package B release promotes approved schema-v2 candidate `20260809T022801911Z-51956`, adds the exact-byte publisher, and records transactional rollback.
- The `WPB-PUBLISH` checkpoint is `ea4e373`, titled `Publish reviewed schema-v2 game data`.
- `WPC-MIGRATIONS` is committed at `5568aa5`, titled `Add isolated character migration registry`.
- The `WPC-REPOSITORY` checkpoint is `44ada92`, titled `Implement definitive character persistence boundary`; it strengthens the existing database reader/writer rather than adding a duplicate repository implementation.
- The current Work Package C session checkpoint is `WPC-SESSION`, titled `Add canonical character session lifecycle`; it remains pure and does not switch deployed pages.
- The current Work Package D graph checkpoint is `WPD-GRAPH-CORE`, titled `Add deterministic character graph core`; it remains fixture-integrated and does not switch deployed pages or publish data.
- The current Work Package E implementation checkpoint is `d035a40`, titled `Migrate builder domains to character session graph`; focused signed-in acceptance remains deferred, so this commit does not remove compatibility paths or deploy production.
- The earlier 2026-09-18 handbook formatting repair cleared linked-table formatting overrides using **Match spreadsheet data and formatting**. All 85 then-existing techniques followed the display Sheet's rich-text title/access/body formatting. Three subsequent ordinary refreshes, including paragraph-count changes, passed; source values, formats, and rich-text runs were restored exactly.
- The later 2026-09-18 operational checkpoint migrated 12 complete handbook techniques into canonical source, preserved 16 editorial weapon records in `TechniqueDrafts`, linked the Rank 0 and new technique excerpts, and repaired feat display imports and native handbook links. The catalogue now contains 97 techniques; verified feat tables contain 19 class feats and 43 archetype memberships. The workflow is recorded in [data-pipeline.md](data-pipeline.md#handbook-display-and-linked-table-formatting). No application code or production data changed; the active Work Package E acceptance boundary is unchanged.
- The 2026-09-19 follow-up removed imported minimum row heights and redundant archetype prerequisite lines, normalized stray grant prefixes, and repaired accidental list/formatting overrides in linked handbook tables. The ordinary-refresh regression and final handbook audit passed: all 62 feat/archetype entries match display text and bold styling, and all 21 relevant tables have zero positive row minima. This changes display/handbook formatting only; the canonical source and active Work Package E boundary are unchanged.
- Agent guidance now requires automatic human-readable briefings before every named roadmap step and self-contained plain-language outcome summaries afterward; filenames, test lists, and jargon cannot substitute for those explanations.
- The 2026-09-19 authoring consolidation moved archetype membership into `Feats.archetypeKey`/`archetypeName`, included all 49 top-level archetype feats, and migrated 15 unfinished weapon techniques into `Techniques` without duplicating Deflect Projectile. The obsolete canonical `ArchetypeFeats` and `TechniqueDrafts` tabs were removed after readback and dependency verification. Existing display tab IDs and excerpt columns were retained. The handbook links now cover all 112 techniques, 22 class feats, and 49 archetype feats. The user explicitly approved deferring exporter compatibility; no runtime data, application code, or deployment boundary changed.
- At the beginning of the schema-v4 synchronization work, the Work Package B branch was clean and two commits ahead of `master`.

Always verify these statements with `git status` and `git log`; update this section after each checkpoint commit.

## Completed

### Milestone 0 and Work Package A

- Preview errors block and destructive dependency removals require confirmation.
- Class changes no longer pre-clear graph-owned dependent state.
- Dazzling Wand technique answers and generated weapons are source-owned and graph-reconciled.
- Character-sheet writes are restricted to temporary sheet-owned leaves.
- Saves are serialized; dirty, saving, failure, and retry state is visible.
- Shared dialog lifecycle and dirty-navigation/unload protection are implemented.
- Identified unsafe data-derived HTML paths and low-risk route/asset defects are repaired.
- Privileged credential paths are rejected when they resolve inside the repository.

Evidence: [work-package-a-completion.md](work-package-a-completion.md) and [milestone-0-completion.md](milestone-0-completion.md).

### Work Package B foundations

- `WPB-BASELINE` is complete: the checked-in production release is hash/count baselined and production export is frozen.
- The canonical source Sheet and display Sheet were normalized and validated on 2026-08-03/04.
- The canonical workbook declares source schema v4, grant syntax v2, and prerequisite syntax v2.
- The repository now has an agent entry point, living roadmap/status, refreshed architecture/data contracts, and automated read-only source acquisition.
- The source snapshot and staging artifacts are ignored; credentials are never stored in the repository.
- `WPB-EXPRESSIONS` is complete: exporter validation and runtime loading share typed grant/prerequisite registries, pure contextual parsing/serialization, resource capacity semantics, and schema-v4 fixtures.
- `WPB-SOURCE-ACCESS` is complete: the dedicated Viewer-only Sheet identity is available through short-lived user-ADC impersonation, without a persistent key.
- `WPB-ADAPTERS` and `WPB-REFERENCES` are complete. Authenticated canonical Drive version `642` adapts and validates with zero errors, 28 intentional warnings, deterministic finding order, and successful runtime-load acceptance.
- `WPB-STAGING` is complete. Runtime revision identity uses the stable Drive revision plus normalized-model hash; byte-distinct XLSX transports for unchanged Drive version `642` now produce byte-identical nine-artifact runtime sets. Authenticated end-to-end staging passes and leaves production untouched.
- `WPB-SOURCE-RESOLUTION` is complete. Descriptions are optional, implicit source-owned choice identities are accepted, `draft` is excluded from normal and grant-owned selectors, and generic `rank`/reversible `choice-rebind` contracts preserve the approved meaning. The user-approved canonical-Sheet batch was applied and read back on 2026-08-05; authenticated 2026-08-08 staging confirms zero structural errors and 28 intentional warnings. See [game-data-source-resolution.md](game-data-source-resolution.md).
- `WPB-DIFF-REVIEW` is complete. The user approved the exact nine-artifact candidate `20260809T022801911Z-51956`; its identity and hashes are recorded in [game-data-release-candidate-2026-08-08.md](game-data-release-candidate-2026-08-08.md).
- `WPB-PUBLISH` is complete. The separate publisher verified both approval gates and exact source/model/artifact hashes, installed only the approved bytes, removed stale `export-report.json`, added `class-skills.json`, refreshed the exact baseline, and retained the generic production-export freeze. Release and rollback evidence is in [game-data-release-2026-08-09.md](game-data-release-2026-08-09.md).

### Work Package C character contract

- `WPC-CODEC` is complete: schema version 5 now has independently allocated canonical defaults and a pure exact codec that reports structured path-specific diagnostics instead of silently coercing malformed state.
- The approved boundary treats schema versions 1–4 as migration input, keeps Firestore timestamps outside canonical state, preserves `visitedSteps` as state, requires stable keys for persisted game-data selections, and exactly allowlists character-sheet fields and repeatables.
- Identity-bearing bond, ability, weapon, enhancement, grant-choice, and resource records are explicit. Existing compatibility snapshots remain transitional fields so later migrations and reconciliation can prove parity.
- The live `database-reader.js` and `database-writer.js` deliberately remain on schema version 4. Switching their version stamp outside `WPC-REPOSITORY` would let partial legacy saves falsely claim to be valid v5 documents.
- The approved reversible `choice-rebind` behavior remains documented, but its original-plus-overlay persistence shape is deferred to the later feature vertical slice rather than guessed in the codec.
- [character-data-contract.md](character-data-contract.md) is the living v5 contract. Agent guidance now also requires decision requests to explain the underlying problem and consequences before offering options or a recommendation.
- `WPC-MIGRATIONS` is complete: historical saved-document compatibility for the target v5 path is isolated in pure `CharacterMigrations`; pages, widgets, rules, graph code, sessions, and the future repository's canonical logic must consume only v5. Existing transitional v4 compatibility branches remain live only until their callers move behind `WPC-REPOSITORY` and later vertical integration.
- Executable history supports unversioned, v1, v3, and v4 inputs. No schema-v2 writer existed, so explicit v2 is reserved and rejected rather than guessed.
- Historical display/composite references require a supplied reviewed-game-data lookup. Missing, unresolved, and ambiguous mappings fail with exact diagnostics; deterministic generated identities and collisions are reported.
- Migration returns exact codec-accepted v5 separately from `createdAt`, `updatedAt`, and historical `lastVisitedAt` repository metadata. Populated legacy values without a lossless v5 binding fail instead of being discarded.
- [character-migrations.md](character-migrations.md) is the living backward-compatibility contract. The live persistence path remains transitional until `WPC-REPOSITORY` owns migrate-then-decode and stamps every successful canonical write as v5.
- Migration write-back policy is approved: opening a historical character migrates it in memory without touching Firebase; the v5 replacement is persisted only when the user explicitly saves successfully.

### Work Package C persistence boundary

- `WPC-REPOSITORY` is active. The existing `database-reader.js` and `database-writer.js` are now the definitive v5 persistence entry points; `character-persistence.js` holds their shared Firebase-free envelope, patch-ownership, revision, and error rules.
- Reads run raw Firestore data through `CharacterMigrations` and the exact v5 codec and return canonical state separately from timestamps, historical visit time, revision, and migration evidence. Migrated reads never write.
- Creates, full replacements, narrow builder/sheet patches, and deletes use exact validation. Successful creates/saves stamp v5; patches apply to the latest value inside a Firestore transaction; full replacement, patch, and delete require a matching document-wide revision.
- Historical documents without a revision are revision 0, new documents begin at 1, and accepted saves increment once. Stale writes raise a typed conflict without changing the newer stored value.
- The character-sheet temporary-leaf allowlist remains exact, arbitrary page paths are rejected, owner/path mismatch is rejected, missing documents are explicit, and Firebase authorization errors propagate.
- Focused emulator tests prove create/read, timestamp resolution, migrated read without write, explicit-save migration write-back, valid and invalid patches, newer-value preservation after a conflict, missing documents, and authorization propagation.
- Deployed page integration is not complete. The published runtime now supplies stable `techniqueKey` values, satisfying the data prerequisite, but existing pages still use clearly marked v4 helpers or direct Firebase calls until their affected Work Package E domain integration is complete. The v4 path must not be removed or falsely stamped v5 before that integration is accepted.
- [character-persistence.md](character-persistence.md) is the living read/write/revision contract and is now part of the required agent startup reading for persistence work.

### Work Package C character session

- `WPC-SESSION` is complete: `CharacterSession` privately owns exact v5 persisted, working, proposed, and reconciled state and exposes only protected projections.
- Strict `SetClass` and `SetTechniqueSelection` commands express direct user intent without dependency policy. Unknown commands/fields, malformed stable keys, and duplicate technique identities fail explicitly.
- One proposal identity records the proposed state, one reconciler invocation, exact reconciled state, deterministic diffs, and structured error/confirmation/information impacts. Pending proposals cannot be silently superseded; cancellation changes no working state; acceptance commits the exact reviewed result without rerunning reconciliation.
- Exact save snapshots carry the expected persistence revision. Successful acknowledgement advances persisted state to what was actually written while preserving newer accepted edits as dirty working state.
- The reconciliation boundary is injectable and defaults to identity behavior. Work Package D now supplies an explicit graph adapter; current pages are intentionally unchanged until their domain slice migrates.
- [character-session.md](character-session.md) is the living state/command/proposal/impact/save contract and is required startup reading for session work.

### Work Package D graph core

- `WPD-GRAPH-CORE` is complete: `GraphCompiler` converts an exact schema-v5 character plus normalized runtime schema-v2 fixture data into deterministic frozen typed nodes, edges, diagnostics, and metadata.
- Every selected class/technique answer in the initial fixture slice has stable identity, source ownership, and an exact scalar, ordered-key-array, or keyed-record storage binding. Populated domains without a registered vertical handler fail explicitly.
- Independent node, grant, and prerequisite handler registries make extension a domain-registration task rather than a traversal rewrite. The initial grant slice covers direct and source-owned technique choices; typed shared prerequisite evaluation remains the Rules boundary.
- `GraphReconciler` applies unavailability, prerequisite, orphan-removal, normal-technique capacity, and incomplete-selection policy to a bounded deterministic fixed point. Removals require confirmation, compiler failures remain blocking errors, and valid incomplete choices remain informational.
- Duplicate identities, dangling references/edges, cycles, missing handlers, handler failures, and non-convergence produce structured diagnostics without hangs or partial reconciled characters.
- A `CharacterSession` adapter supplies the graph result through the existing injected boundary exactly once per proposal. Acceptance commits the reviewed fixed point and cancellation remains byte-for-byte side-effect free.
- [character-graph.md](character-graph.md) is the living graph/compiler/reconciler and domain-extension contract.

### Work Package E domain migration

- `WPE-DOMAIN-MIGRATION` is active at its browser-acceptance boundary. Automated local-review slices cover the class page's class, level, primary attribute, class options, feats, feat options, and source-owned grant answers; normal techniques; ordinary/source-owned equipment; all six Attributes; Origin selection/Keystone text/Origin feature sources; fixed, custom, class-utility, defense, and granted Skills; and ordinary/source-owned Bonds plus Bond/Background Keystones.
- Those controls now submit narrow typed commands to `CharacterSessionPage`; `CharacterSession` protects proposal/cancellation/acceptance state; the one graph facade owns prerequisite, capacity, orphan removal, generated weapon/resource/Bond state, derived abilities, and granted-skill reconciliation.
- The migrated pages read and replace exact schema-v5 characters through the definitive revision-aware database reader/writer. `VisitBuilderStep` records navigation state through the session rather than a separate Firebase patch. Equipment no longer holds a mutable `currentWeapons` page model; Attributes no longer owns DOM-derived character state or point pruning; and Origin/Skills no longer build broad patches, call the transitional dependency refresher, or calculate selection/capacity rules inside their pages.
- Portable widgets still own DOM, accessibility, and interaction behavior. They do not import Firebase or persistence modules and do not own a second character model. Page-level prerequisite/capacity checks are presentation hints from shared pure Rules; enforcement and dependency removal remain graph-owned.
- The graph accepts the published stable-key contract, including underscore-bearing weapon keys. Regression tests reconcile every selectable class and every selectable weapon base against the published combined runtime artifact.
- Equipment commands address stable weapon/enhancement identities and cannot edit ownership fields. Graph nodes own weapon/enhancement references, rank and compatibility rules, four-slot weapon capacity, enhancement capacity, generated-weapon synchronization/tamper rejection, incomplete enhancement answers, and confirmed dependent rank/removal effects.
- `SetAttributeValue` addresses one recognized attribute key and integer value. The shared pure `getAttributeAllocationState` Rules projection is the single source for point capacity/usage/remaining, primary minimums, level caps, and currently assignable maxima consumed by both graph compilation and widget presentation; deterministic over-budget fitting also remains in Rules. Graph fact nodes retain exact scalar bindings, destructive adjustments require review, and unspent points remain informational.
- `SetOrigin`, `SetOriginKeystone`, `SetClassUtilitySkills`, `SetSkillRank`, `SetCombatSkills`, and `SetSettingSkills` express only direct Origin/Skills intent. `selection-rules.js` now owns normal/granted-only/draft eligibility and `getOriginSelectionState` applies it to Origins. `skill-rules.js` is now the sole home of skill progression breakpoints, grant-derived ranks, utility-choice capacity, cap bonuses, level rank caps, point capacity/usage/remaining, assignable maxima, and deterministic cap/budget fitting; the duplicate mechanics formerly in `game-data.js` and the Skills page were removed.
- Origin and skill graph nodes retain source ownership and exact scalar/named-collection bindings. Origin features compile as active graph sources, their grants flow through the existing registries, source-owned ability removal is reviewed, unavailable Origins and excess class utility choices reconcile deterministically, and destructive rank fitting requires confirmation. Unspent points and incomplete Origin/Keystone/utility selections remain informational.
- `AddBond`, `RemoveBond`, `UpdateBond`, and `SetBackgroundKeystones` express exact direct intent. `bond-rules.js` is the sole source for Heart-based user capacity, level rank caps, two Background Keystone slots, source-owned exclusions, and deterministic repair used by both graph and widget.
- The graph compiles stable Bond/Keystone identities and materializes the published Artifact Rank 2 and Patron Rank 1 Bonds as source-owned additions outside Heart capacity. Source ranks and removal stay owned by the grant; relationship name and Keystone remain editable. Removing the source reviews both the Bond and derived ability, and cancellation is side-effect free.
- Existing ordinary Bonds are never guessed to be an Artifact/Patron grant answer because v5 has no historical ownership evidence for that inference. A character that previously entered the same relationship manually may temporarily show both records; the ordinary record remains user-owned and removable, while the generated record carries the source binding.
- The Boon extensibility proof adds an isolated Rules adapter, automatic node/grant extension, portable widget, and generic widget registry. Existing page controllers and graph traversal contain no Boon branch; no selectable Boon content or new persisted field was invented without a source contract.
- JavaScript responses require cache revalidation, and all seven migrated HTML entry points use a migration version on their module roots so returning browsers cannot combine cached pre-migration dependencies with the new graph/session path.
- Focused browser-independent acceptance is green. The user resumed signed-in browser acceptance on 2026-08-30. Loading historical character `oQvPBy4LCN6GxGYEGvKR` on the Class page initially failed exact-v5 decoding; the local fix now recognizes prior account-import bookkeeping, rebinds the Dazzling Wand option/answer to reviewed stable identities, and displays Prismatic Burst as selected. The subsequent save attempt correctly remained unsaved but exposed duplicate `autoAbilityNames` output for the two distinct Magical Guardian Feat sources. Graph reconciliation now deduplicates only that non-identity display snapshot while preserving both stable ability records. The raw emulator document remains unchanged until an explicit save. Remaining Class/Feat/Technique and other domain scenarios—including the successful explicit save/reload, destructive confirmation/cancellation, conflict messaging, and console checks—are still pending; no compatibility path was removed.

## Canonical external source

| Property | Current value |
|---|---|
| Source Sheet | [game-x-class-data](https://docs.google.com/spreadsheets/d/1TEdxuufglP8lFRNk8QD4N_351-0ihAUFLG2743ESjoI/edit) |
| Drive file ID | `1TEdxuufglP8lFRNk8QD4N_351-0ihAUFLG2743ESjoI` |
| Published release Drive version | `647` |
| Published release Drive modified time | `2026-08-09T02:27:03.310Z` |
| Current source checkpoint | Native XLSX exported after the duration clarification; source modified `2026-09-21T18:05:43.676Z`, unchanged across export, Drive version unavailable; 150 Techniques and 60 Traits preserved in the v5 integration snapshot |
| Source schema | `5` |
| Grant syntax | `3` |
| Prerequisite syntax | `3` |
| Display Sheet | [Game-X-Data-Display](https://docs.google.com/spreadsheets/d/106wXA3w52aubp0zCYqieHJME02C0bu4jdho9b_eBA8U/edit) |

The repository's old `data/game-x-class-data.xlsx` is a June 29 snapshot and is not authoritative. Use `npm run fetch:data` to acquire a fresh ignored snapshot.

## Current boundaries

- Production JSON is the reviewed schema-v2 release from canonical Drive version `647`, exact-hash baselined across nine runtime artifacts.
- The staging CLI uses the canonical reader -> versioned adapter -> validator -> schema-v2/v3 builder -> runtime acceptance -> diff pipeline. Source v5 produces ten schema-v3 artifacts, including Traits; the v4 path retains exact schema-v2 compatibility. It never invokes the legacy exporter or publisher.
- The generic exporter remains frozen. Only `npm run publish:data -- --confirm <approved-run-id>` may promote an exact separately approved staging run.
- Character schema v5, its migration registry, the definitive Firebase reader/writer APIs, the pure four-state session lifecycle, and the deterministic graph compiler/fixed-point reconciler are implemented and tested. Every local-review builder domain uses them; production cutover and transitional-code removal remain blocked on deferred focused acceptance.
- Runtime stable technique keys are available. The remaining Work Package E gate is behavioral browser acceptance, not domain implementation or data identity.
- Familiar, vehicle, and gadget expressions are preserved with explicit runtime-stub status until their future subsystem slices; they are no longer rejected or discarded by data loading.
- The graph core accepts normalized runtime artifact schema 2 or 3, preserving exact character schema v5. The published release is still artifact schema 2. Typed unsupported effects remain deferred; a successful import does not add new character state for conditional evolution, generic Keystone answers, recipient-owned skills, or equipment hand assignments.
- During the earlier spreadsheet-normalization checkpoint, the Handbook remained untouched because its bound import script was inaccessible. The 2026-09-18 work subsequently regenerated native display outputs and updated the handbook's linked tables; that historical restriction no longer describes the current handbook.

## Deferred acceptance and blockers

### Manual acceptance debt

- Manual browser scenarios in [work-package-a-completion.md](work-package-a-completion.md) remain pending: destructive reconciliation, capacity changes, source-owned choices/weapons, dialog focus, dirty navigation, unload prompt, save retry, and two-tab isolation.
- On 2026-08-05 the user explicitly overrode this gate for a production Firebase Hosting deployment of the current committed `public/` tree. This override does not authorize game-data publishing, Firebase Rules/Functions deployment, or removal of the outstanding acceptance debt.

### Production data publishing

- No publishing blocker remains for candidate `20260809T022801911Z-51956`; it was approved and promoted through the exact-byte publisher.
- The v5 import resolves the earlier format incompatibilities and produces a staged diff. Release review must address removed source identities, weapon-profile retirement, and explicitly deferred runtime mechanics. The publisher/release contract still admits only the nine-file schema-v2 release; a ten-file schema-v3 promotion requires a tested contract update and separate exact-byte approval under `WPB-V5-RELEASE-REVIEW`.
- Future source changes require a new immutable run, zero-error validation, complete semantic diff review, an exact release contract, and separate publish approval. The generic production export freeze remains active.

### Automated source acquisition

- Current September 21 CLI access check fails with insufficient read-only Drive access. The v5 integration uses an independently authorized connector native export, checked against unchanged source modification time and explicit null Drive version. `stage:data --snapshot ... --provenance ...` verifies canonical identity, versions, actual workbook Metadata, byte length, and SHA-256 before staging. This does not claim that default CLI acquisition has been restored.
- `WPB-SOURCE-ACCESS` completed on 2026-08-08 with `game-x-sheet-exporter@game-x-character-builder.iam.gserviceaccount.com` shared as Viewer on only the canonical Sheet.
- Local user ADC impersonates that identity; the repository requests a 15-minute Drive-read-only target token.
- No persistent service-account key was created and no credential file exists under the repository.
- Set `GAME_X_DATA_IMPERSONATE_SERVICE_ACCOUNT` to the service-account email in each acquisition shell before running the documented commands.

See [data-pipeline.md](data-pipeline.md) for exact commands and security guidance.

## Last verification

Schema-v5 importer integration on 2026-09-21:

- `npm run test:all`: **348/348 unit tests**, **16/16 emulator rule tests**, **14/14 HTML asset checks**. Final log: ignored `.staging/schema-v5-integration/final-full-suite.log`.
- `npm run baseline:data`: all **nine** production artifacts unchanged. `git diff --check`: passed.
- `npm run stage:data -- --snapshot .staging/schema-v5-integration/game-x-class-data.xlsx --provenance .staging/schema-v5-integration/source-provenance.json`: successful run `20260921T183411964Z-31844`, **ten artifacts**, **0 errors / 300 warnings**, runtime acceptance passed. The exact verified input pair is retained in memory through staging; a regression proves swapped disk files cannot replace it after verification.
- Independent source comparison: **951 records / 13,901 cells** preserved, all four Weapon Master conditional relationships retained, new Technique ranks retained while unknown readiness/cost stays unknown. Final artifact hashes equal the previous independent run. Fresh in-memory candidate compilation also covers all seven currently playable classes at level 12 without graph errors; deferred mechanics remain explicit warnings.
- Local Firebase restart preserves emulator data and serves updated modules at `http://127.0.0.1:5000`. Signed-in acceptance and schema-v3 production publishing remain separate pending boundaries. The CLI Drive-access failure remains external; the verified connector native export does not claim to restore ADC access.

Handbook formatter activation and setup repair on 2026-09-20:

- Preflight passed **256 unit tests**, **9 frozen artifact checks**, and **14 HTML asset checks**. Final verification passed **7 focused formatter tests** and **257 repository unit tests**, including editor execution without document UI, opaque Docs trigger source IDs, duplicate cleanup, unrelated-trigger preservation, and busy-installation refusal.
- The bound project's **Docs v1** service is saved. The corrected installer completed at **12:03:49 America/New_York**; the trigger page showed exactly **one** `onHandbookOpen` document-open trigger, no timer, and a **0%** error rate. Automatic-open execution completed in **56.514 seconds**, and the manual **Handbook → Format tables** execution completed in **64.98 seconds**, including its success dialog.
- Final readback at **2026-09-20T16:06:12.403Z** verified **50 tables**, **459 rows**, **1,167 cells**, and **4,557 table paragraphs** across four tabs. All widths, row minima, alignment, stripes, padding, and four border edges passed. Table text, emphasis, hyperlinks, list metadata (37 native list paragraphs), dimensions, and indexes matched the pre-run snapshot exactly. Evidence is ignored under `.staging/handbook-activation-2026-09-20/`.
- No source edit, runtime-data publication, or website deployment was needed. Concurrent Trait/display authoring changes were preserved outside this fix. The next named step remains **`WPE-DOMAIN-MIGRATION`** focused signed-in acceptance: verify saving/reloading, dependency-change confirmation/cancellation, and conflict/focus handling before clearing the existing production-deployment boundary.

Repeated availability and zero-Energy wording cleanup on 2026-09-19:

- Before edits, `npm test` passed **256 unit tests**, `npm run baseline:data` matched all **9 frozen artifacts**, and `npm run validate:assets` passed all **14 HTML files**.
- Guidance records the exact approved scope: remove the repeated provider/availability output, including draft intended availability, and remove only `0 Energy before optional pumping.` from the 23 basic-attack source notes. Preserve the four Rank 0 no-pumping notes, other meaningful notes, skill Access lines, and incomplete/missing-mechanic notices. This does not change automatic weapon-provided access, draft selection restrictions, costs, damage, or pumping values.
- Source readback verified exactly **23 changed AA (`rankNotes`) cells**: **19 blank notes and four preserved Rank 0 no-pumping notes**. All other source cells across **139 technique records** are unchanged. Evidence is ignored at `.staging/weapons-migration-2026-09-19/redundant-lines-source-readback.json`, SHA-256 `33154344F135650A1BD00F929F8AAFB682A7B796791AA04597A414BF3759A2B6`, exported `2026-09-19 18:00:06 UTC`. Observed source modification time is `2026-09-19T17:58:02.544Z`; Drive version is unavailable. This connector readback is authoring evidence, not runtime-source acquisition or a staged release candidate.
- All **32 native Function_Tests passed**, including compact output for **23 basics** and exact retention of all **13 nonempty rank notes**. The three redundant line types are absent from all **139 rendered techniques**, all **31 weapon-base cards**, and handbook tables.
- Native handbook readback matched **153/153 entries**—31 base cards, 108 main techniques, and 14 generic excerpt entries—in text and every non-whitespace character's bold/italic flags. All **50 tables** have **468-point width**, with **459 rows at zero minimum height** and **1,167 cells**. Three **Match spreadsheet data and formatting** repairs and **34 layout requests** were scoped to three changed weapon tables. Native visual review confirmed the readable Longsword card with the three redundant lines removed. Evidence is ignored at `.staging/weapons-migration-2026-09-19/redundant-lines-final-verification.json`; final handbook revision is `ANLCKQnDxoaU2_ISCkCNH8LPF1fi7z-gQrzmysEPQcrMrdfEC-fWlyDoRE036eNzHzuVwwG0mfBnhtPPWg`.
- The cleanup changed no application code or runtime artifacts and performed no deployment. Earlier compact-format verification below remains historical evidence. Next step **`WPE-DOMAIN-MIGRATION`** still requires deferred signed-in save/reload, dependency confirmation/cancellation, and conflict/focus acceptance. Exporter/runtime integration, staged diff review, runtime publish approval, and production deployment boundaries are unchanged.

Compact technique-format correction on 2026-09-19:

- Source readback verified exactly **69 cells in columns R/Y/AA across 23 basic attacks**: compact `damage`, blank `damageByRank`, and concise Energy/Rank 0 `rankNotes`. Every cleared damage-map value was evaluated against its compact linear formula and matched. Pumping maps are untouched, and all source cells outside those 69 changes across the **139 observed technique records** are unchanged. Spirit Blast and Spacium Ray retain the user's compact examples, distinct pumping schedules, and meaningful costs/benefits.
- All **32 native Function_Tests pass**. The shared renderer groups adjacent ranks only when pumping coefficients and units match, preserves gaps and separate runs, and retains explicit irregular damage-map fallback. Formatted content is current for **108 main catalogue entries, 14 generic weapon-excerpt entries, and 31 weapon-base cards**; source technique ownership and the 32 base references are unchanged.
- Native handbook refresh and final readback verified **153/153 entries**—31 base cards, 108 main entries, and 14 generic excerpt entries—against current Sheet text and every non-whitespace character's bold/italic flags. All **50 tables** have **468-point total width**, with **459 rows at zero minimum height** and **1,167 cells**. The final **34 layout requests targeted only three changed weapon tables**. Native visual review confirmed Longsword's compact damage and single pumping line and the Rank 0 Broomstick entry. Final document revision is `ANLCKQnzLhJrGxTKSbB3woOyoNQi3emMvMjkFhNRL2CwgKMM8muopjlFzyWDJhKgGcxHLXsVwfr1Br6-RQ`; evidence is ignored at `.staging/weapons-migration-2026-09-19/compact-technique-final-verification.json`.
- `npm test` passed **256 unit tests**, `npm run baseline:data` matched all **9 frozen artifacts**, and `npm run validate:assets` passed all **14 HTML files**. Source/display/handbook authoring is the only changed behavior; there is no application-code change, runtime-data publish, or website deployment in this correction.
- Source readback is ignored at `.staging/weapons-migration-2026-09-19/compact-technique-source-readback.json`, SHA-256 **`5A1762511E289DF0224A29413FD1161BCF7EDA028D8DA147E189774CD2DB275F`**. Observed source modification time is **`2026-09-19T17:35:27.287Z`**; Drive version is unavailable. This connector evidence is not authenticated runtime-source acquisition or a staged release candidate.
- Next named step remains **`WPE-DOMAIN-MIGRATION`**: complete the deferred signed-in save/reload, dependency confirmation/cancellation, and conflict/focus acceptance for the session/graph pages. Exporter/runtime integration, staged diff review, runtime publish approval, and production deployment boundaries remain deferred and unchanged.

Weapon-card and automatic-damage refinement on 2026-09-19:

- Source readback verified all **73 approved cells across 23 basic attacks**. Starting damage is preserved; automatic per-rank growth and optional pumping are separate. All basics use variable Energy with a blank numeric cost and explicit 0-Energy base use. Alternative costs remain unchanged, and the user's concurrent Deflect Energy skill/Rank 2 edits were preserved. Missing Rank 2 actions and other incomplete mechanics remain draft.
- Native display verification found **139 shared-pool records, 31 specific weapon actions, and 108 main catalogue records**. All **29 Function_Tests passed**. Formatted output contains A108 main entries, N31 weapon-specific helper entries, and L14 generic weapon excerpt entries. All 32 base technique references resolve through the shared full-block pool, including generic Covering Fire. These are observed source counts; future edits must recompute them instead of restoring fixed totals.
- The six native weapon-base links cover **31 base entries**; the main catalogue covers **108 entries**, and the existing generic weapon excerpt covers **14**. All **153 entries** match their formatted source text and every non-whitespace character's bold/italic flags. Five linked-table formatting overrides were cleared using **Match spreadsheet data and formatting**; the source rich-text formatting remains authoritative. Weapon cards have verified TOP vertical alignment in both the display Sheet and native handbook, and the catalogue introduction distinguishes general alphabetical techniques from weapon-base entries.
- After verifying the scaling source, removed the obsolete **8-row, 6-column Weapon Damage table**. Its heading and the replacement explanation remain. The final handbook has **50 tables, 459 rows, and 1,167 cells**, all at **468-point total width** with **zero positive minimum row heights**. The final style-only batch contained **164 requests scoped to five changed tables**, preserving other content.
- Final verification reran `npm test` with **256 passing unit tests**, `npm run baseline:data` with **9 matching frozen artifacts**, and `npm run validate:assets` with **14 passing HTML files**. The refinement changed no application code or runtime artifacts and performed no website deployment or runtime-data publish. Evidence is ignored under `.staging/weapons-migration-2026-09-19/refinement-final-verification.json`, with the unit log in `refinement-tests-final.log`; the earlier `final-verification.json` records the pre-refinement 139-entry/51-table checkpoint. The authoring readback SHA-256 is `3CD3F4F11FFD83A0ECD9D977A5FBEBF3A9DFFC43D199063CE57EE1C3A974FF63`; observed source modification time is `2026-09-19T17:06:18.955Z`, with no Drive version exposed. This connector evidence is not authenticated runtime-source acquisition or a staged release candidate.
- Next named step remains **`WPE-DOMAIN-MIGRATION`**: complete the previously deferred signed-in save/reload, dependency confirmation/cancellation, and conflict/focus acceptance to establish release readiness for the session/graph pages. The new weapon-source relationships and authoring extensions still need separate exporter/runtime integration, successful staging/diff review, and publish approval. This handbook verification does not remove those boundaries.

Initial weapon techniques and Ranged Weapons migration on 2026-09-19:

- Source readback matched all 1,194 cells in the initial approved batch; a later audit matched all 1,085 fields of the 31 new technique records. All 31 bases have unique keys; 22 bases reference 32 existing techniques (including Machine Gun's existing Covering Fire). The retired profile tab has its exact header, zero records, and hidden status. The subsequent eight terminology-only cell edits were read back separately. Snapshots, modification times, edit plans, and hashes are ignored under `.staging/weapons-migration-2026-09-19/`.
- Both bulk native formatter runs completed all seven display pairs while retaining existing destination IDs. The new WeaponBases output has ID `1175877209`; its 31 catalogue cards and six rank excerpts (4/14/4/5/1/3) resolve every technique name. Native handbook links preserve table semantics. The final catalogue refresh preserves the user's subsequent two deletions: `Techniques_Formatted!A1:A139` matches all 139 handbook rows, including non-whitespace bold/italic flags. L14 and N31 are unchanged, all 24 Function_Tests pass, and all 31 base cards match their linked handbook tables.
- Handbook rules describe automatically provided weapon techniques and normal critical-success riders, including the optional normal-damage-plus-named-alternative effect. All remaining capitalized Targeting text identifies the unrelated Enhanced Targeting enhancement or Targeting Computer, rather than the skill. The final layout audit verified 51 tables, 498 rows, and 1,246 cells at 468-point total widths with zero positive minimum row heights. A style-only target-revision retry preserved every table's text and emphasis while concurrent unrelated prose edits were retained. No temporary insertion markers remain.
- `npm run test:all` passed **256 unit tests, 16 emulator rule tests, and 14 HTML asset checks**. The final display/handbook formatter check passed **9/9 tests**. `npm run baseline:data` verified all **9 frozen artifacts**, and `git diff --check` passed. `npm run local:restart` refreshed the review environment at `http://127.0.0.1:5000` (emulator UI `http://127.0.0.1:4000`). No production deployment or runtime-data publish occurred.
- Next named step remains `WPE-DOMAIN-MIGRATION`: finish signed-in save/reload, dependency confirmation/cancellation, and conflict/focus acceptance so the session/graph implementation can be approved for release. The new source relationships require separate exporter/runtime compatibility work, successful staging, and publish approval before automatic weapon techniques reach production.

Authoring consolidation on 2026-09-19:

- All 73 existing feat/option rows retain their original fields except 13 prerequisite key corrections to the preserved archetype identities; membership/label fields were added in N:O. The 97 existing technique records retain every mechanic; only Deflect Projectile's notes/provenance gained its original handbook reference. All 97 pre-existing catalogue display entries are byte-identical. Fifteen new draft records preserve their original body text, unresolved questions, stable keys, and provenance; there are 112 unique technique keys.
- Derived membership covers all 49 top-level archetype feats, including the six previously omitted Weapon Master feats. Nested options inherit membership. All six formatted pairs regenerated successfully, retaining every existing output sheet ID and D/F/H/J/L technique excerpt. Updated stale fixed-count checks to compare catalogue/display/formatted coverage; all 24 Function_Tests checks pass.
- Expanded native handbook links to `Techniques_Formatted!A1:A112`, `Archetypes_Formatted!E1:E9`, and `Feats_Formatted!E2:E8`, and refreshed the 16-entry weapon excerpt. All 214 technique/catalogue/excerpt/class-feat/archetype output entries match handbook text and non-whitespace bold/italic flags. A whole-table **Match spreadsheet data and formatting** cleared two all-bold Docs overrides introduced/exposed during range expansion; no individual-cell typography patches were used.
- Final layout audit: 45 tables, 442 rows, and 1,190 cells; zero positive row minima; normal 468-point total widths; left-aligned paragraphs; alternating grey/white rows; 0.75-point light-grey borders. All 109 genuine native-list paragraphs remain. Most formatting writes used required-revision guards; one table-only retry used Google's target-revision merge after repeated revision conflicts, followed by full content/style verification.
- Preflight passed all 247 unit tests and the frozen nine-artifact production baseline. No exporter, generated runtime artifact, or application code changed, so there was no live staging, runtime publishing, or website deployment. Recovery snapshots, installed formulas, source modification time, and SHA-256 manifests are ignored under `.staging/authoring-migration-2026-09-19/`; installed formula definitions are maintained in `scripts/display/authoring-formulas.json`. The next roadmap boundary remains `WPE-DOMAIN-MIGRATION` focused browser acceptance, independent of these authoring changes.

Bulk display-sheet formatting on 2026-09-19:

- Added **Format Sheet → Update all Display → Formatted tabs** to the existing Game-X-Data-Display bound project, preserving both individual-tab commands and the existing markdown parser. The maintained extension is [scripts/display/DisplayBatch.gs](../scripts/display/DisplayBatch.gs), with installation/operation notes beside it. No new API service, scope, or trigger was needed.
- A native menu execution completed all six pairs: Classes, ClassFeatures (hidden), Techniques, Feats, Origins, and Archetypes. It created hidden `ClassFeatures_Formatted` (sheet ID `2044388241`) and visible `Origins_Formatted` (`234578775`); all four existing output IDs were retained. Readback matched all 502 nonempty output cells to converted source text, with no formula errors, source-cell changes, or lost technique excerpt columns. Current class-feat output has 22 entries. Native visual review covered all five visible outputs; hidden output content/styles were checked through the connector. The completion dialog reported all six destinations, and the original active tab was restored.
- `npm test` passed all 247 tests, including three new checks for complete discovery, existing-output reuse, hidden output handling, internal blank/excerpt preservation, failure before writes on source errors, and accurate partial-failure reporting. Saved cloud extension code matches the checked-in source. Evidence is ignored under `.staging/display-batch-2026-09-19/audit.json`.
- The command consumes current calculated values and does not force `IMPORTRANGE` refreshes. Native handbook links still require **Update all** in Google Docs. The separate handbook on-open formatter activation remains pending its earlier Google terms approval and authorization. No canonical source, runtime JSON, or website changed; the application boundary remains `WPE-DOMAIN-MIGRATION`.

Uniform handbook table formatting on 2026-09-19:

- Added the bound formatter source, manifest, setup notes, and six focused tests under `scripts/handbook/` and `tests/handbook-formatter.test.mjs`. The saved cloud code matches the prepared local source. The existing handbook is already formatted; activating the Docs advanced service and the installable open trigger still requires the pending Google terms approval and authorization. No clock trigger is planned or installed, and Google exposes no native linked-table-refresh event.
- The same request planner was exercised through direct connector writes, first on one table and then on all 45 tables across four tabs. Readback verified 418 rows and 1,166 cells: 468-point table widths, preserved column proportions, alternating `#f2f2f2`/white rows, zero borders and row minima, 3-point vertical/4-point horizontal padding, and left-aligned paragraphs. Native Table options verified effective top alignment. All 3,896 table paragraphs retained their text/emphasis, including 109 native list paragraphs and their hierarchy/indentation. Evidence: `.staging/handbook-uniform-2026-09-19/final-style-audit.json` and the trusted before snapshot.
- The subsequent approved border adjustment replaces the zero borders with 1px-equivalent (0.75-point) `#e0e0e0` borders in all 45 tables and the saved bound formatter. Readback verified all 4,664 cell edges and exact preservation of all non-border content/styles; native visual review passed. Evidence: `.staging/handbook-uniform-2026-09-19/border-style-audit.json`. The six formatter tests passed after this adjustment, and preflight again passed all 244 unit tests, nine frozen artifacts, and 14 HTML asset checks. Automatic activation remains pending the same Google service-terms approval and authorization.
- `npm test` passed all 244 tests; the six new checks cover width/proportion calculation, target/tab handling, content/list preservation, revision-safe writes and lock release, failure-safe installation, and duplicate-free on-open installation. Preflight also passed the nine frozen data artifacts and all 14 HTML asset checks. No canonical Sheet, runtime JSON, website code, or deployment changed. `WPE-DOMAIN-MIGRATION` remains the application boundary.

Handbook spacing and duplicate-prerequisite follow-up on 2026-09-19:

- Preflight passed on Node.js `v22.22.1`: all 238 unit tests, nine frozen artifacts, and 14 HTML files passed. The starting worktree was clean at `80c34de` (`Document linked handbook feat and technique migration`).
- Range expansion had added positive row-height minima to new rows while original rows retained zero minima. All 51 affected rows across nine tables were reset in bulk; the largest minimum was 982.5 points for a short Dark Witch entry. All 21 relevant tables currently have zero minima after ten affected native refreshes and source-format matching.
- Nine display formula anchors (`_Feats!A1`, `Feats_Display!A2`, and `Archetypes_Display!A1:G1`) now resolve referenced prerequisite groups, suppress only matching duplicate metadata, and normalize stray single-line grant prefixes. A native pilot verified 62 records with zero errors: exactly 19 intended text changes (seven duplicate lines and 12 grant prefixes), with the other 43 unchanged apart from concurrent user edits to Ghostwalker and Warden Soul, which were preserved.
- Accidental native bullets were removed from 118 paragraphs in 12 feat cells and their indentation reset to zero. The two Celestial Knight literal choice bullets and 109 other native bullet paragraphs were preserved. An ordinary refresh initially made those 12 cells entirely bold through inherited Docs overrides; **Match spreadsheet data and formatting** cleared those overrides.
- The controlled Weaponsmith ordinary-refresh regression added one temporary newline before Grants, preserving bold title, regular body, no native bullets, and zero row minimum. Exact restoration of the Sheet value, format, and rich-text runs passed JSON equality and was applied with ordinary **Update Table**. Native visual review confirmed correct typography and a compact row. Evidence: `.staging/handbook-compact-2026-09-19/refresh-regression.json`.
- The final handbook audit passed: all 62 feat/archetype entries match current formatted display text and every non-whitespace character's bold flag; all titles are bold and the 12 affected bodies are regular. Those 12 cells have no native bullets, while the other 109 native bullet paragraphs retain exact text and bullet metadata and both Celestial Knight literal choice bullets remain. All 21 relevant feat/technique tables have zero positive row minima, including the nine repaired tables. Evidence: `.staging/handbook-compact-2026-09-19/final-handbook-qa.json`, including the source snapshot hash and final document revision. No canonical source edit, runtime publish, application change, or deployment was performed; `WPE-DOMAIN-MIGRATION` remains the next application step.

Source/handbook migration and display repair on 2026-09-18:

- Preflight passed on Node.js `v22.22.1`: `npm test` ran 238 tests with zero failures; `npm run baseline:data` matched all nine frozen artifacts; `npm run validate:assets` passed all 14 HTML files.
- The 12 prepared technique rows passed the source adapter and model validator with zero diagnostics in a fixture-backed check. An additional editorial `TechniqueDrafts` tab left the runtime technique count unchanged. A blank rank on an actual runtime draft technique was separately confirmed to fail validation, so unknown ranks were preserved editorially rather than invented.
- Canonical `Techniques!A87:AI98` now contains the 12 authored additions. A final read compared every field of the original 85 records: all 2,975 comparisons matched. The 97-record source snapshot SHA-256 is `5ea33fb1f2104f3a8d535a4938d3912307de0199d52adc4966ac2a375aefd6a9`; the source modified time is `2026-09-18T20:17:38.284Z`. The connector did not expose the current Drive version.
- Independent handbook verification matched all 97 catalogue rows to display text after whitespace normalization, with bold titles and zero bold body characters. All 31 new helper rows (three Rank 0, 12 migrated techniques, 16 editorial entries) also matched their display outputs with the intended title/body styling. The catalogue retained its 468-point width within the stored 6.5-inch content area.
- The repaired feat pipeline and refreshed native links matched all 62 handbook rows: 19 class feats, including the migrated Fairy Transformation, and 43 archetype memberships. Titles were bold and body text nonbold; membership consistency was verified.
- Local evidence is retained under `.staging/technique-migration-2026-09-18/`, including source snapshots, exact migration plans, and handbook checks. No live staging run, runtime publish, application change, or deployment was performed. Live-source publishing findings above remain unresolved; `WPE-DOMAIN-MIGRATION` remains the next application step.

Work Package E historical-character acceptance repair verification on 2026-08-30:

- Signed-in local browser verification loaded historical character `oQvPBy4LCN6GxGYEGvKR` at the Class page with no visible error. Magical Guardian, level 5, Attunement, Dazzling Wand, and its source-owned Prismatic Burst technique were restored; the picker value is the canonical `prismatic-burst` key.
- The raw Firestore emulator document was not changed. Exact replay through the reader's migration/reference path returned schema v5 with zero diagnostics and revision 0, while preserving both identity-bearing Magical Guardian Feat ability records.
- Focused migration, persistence, session-page, and graph verification passed 55/55 tests. `npm run test:all` passed 226 unit tests, 16 Firebase emulator Rules/persistence tests, and all 14 HTML entry points. `npm run baseline:data` matched all nine frozen artifacts, and `git diff --check` passed.
- The local Firebase review environment was restarted at PID `30140`, the user was signed back into the local emulator, and the same historical Class-page state was reverified after restart. The imported raw emulator record remains schema v4 with no revision, proving that page load did not write the in-memory migration. App: `http://127.0.0.1:5000`; Emulator UI: `http://127.0.0.1:4000`.
- The first explicit save attempt remained unsaved because reconciliation emitted the shared display label `Class Feature - Magical Guardian Feat` twice into the duplicate-free compatibility snapshot. The repaired reconciler emits that display label once but retains two source-owned ability records with distinct `abilityId` and `sourceId` values. A signed-in same-class command exercised the repaired reconciliation without writing; the visible error cleared and the raw emulator document remained schema v4 with no revision.
- The same-name derived-ability regression is idempotent and passed within the full `npm run test:all` gate: 227 unit tests, 16 Firebase emulator Rules/persistence tests, and all 14 HTML entry points passed. `npm run baseline:data` again matched all nine frozen artifacts, and `git diff --check` passed.
- The next save preview exposed an unfinished boundary rather than a malformed character: typed `feat` grants were preserved as deferred effects while both the Class page and graph compiler separately manufactured slots from `floor(level / 2)`. The user rejected duplicate automatic plus feature-granted capacity and approved explicit feat-granting features as the only source of feat choices.
- `feat-rules.js` now centrally materializes explicit slots from active typed grants, applies type/category/max-level filters, and deterministically assigns the existing ordered `selectedFeats` keys without a storage migration. The graph registers real `feat` grant and `feat-slot` nodes, each selected feat is owned by its matched slot, and removing a granting source requires confirmation. The portable widget imports the same projection; the page and legacy compatibility graph no longer contain a level-derived feat-slot formula. The prior deferred-domain message is no longer emitted for `feat` grants.
- The explicit-feat regression and architecture gates passed within `npm run test:all`: 232 unit tests, 16 Firebase emulator Rules/persistence tests, and all 14 HTML entry points passed. `npm run baseline:data` matched all nine frozen artifacts and `git diff --check` passed.
- The local review environment was restarted at PID `2480`. Signed-in browser verification reloaded historical character `oQvPBy4LCN6GxGYEGvKR` with no visible error, preserved Dazzling Wand/Prismatic Burst, and displayed `Explicit feat choices: 0/3` at Magical Guardian level 5: two class slots and one archetype slot from the three active published feature grants. No Save button was clicked and no character write was attempted at that checkpoint. App: `http://127.0.0.1:5000`; Emulator UI: `http://127.0.0.1:4000`.
- Informational save notices now use centralized exact field ownership for the current builder page. They do not depend on visit history and cannot surface incomplete choices from other pages; blocking structural errors and confirmation-required consequences remain unfiltered. All seven migrated page entry points use this presentation boundary.
- The warning-scope regression passed within `npm run test:all`: 235 unit tests, 16 Firebase emulator Rules/persistence tests, and all 14 HTML entry points passed. `npm run baseline:data` matched all nine frozen artifacts and `git diff --check` passed.
- The local review environment was restarted at PID `33416`. Signed-in browser verification opened the Attributes save preview for historical character `oQvPBy4LCN6GxGYEGvKR`; it listed only `24 attribute points are still unspent.` and omitted the character's incomplete Origin, class feat, archetype feat, source-owned answer, technique, skill, Bond, and Keystone notices. The dialog was cancelled and the page reloaded, so no explicit character save was performed. App: `http://127.0.0.1:5000`; Emulator UI: `http://127.0.0.1:4000`.
- Skills/Bonds browser acceptance exposed two responsibility bugs. Class utility grants were treated as locked final Rank 1 values and reconciliation erased stored ranks for the same fields; widget command-busy code also disabled every control under the page main, capturing page-owned Save actions and leaving both Background Keystone inputs disabled after blur.
- Shared Skill Rules now treat a class utility grant as a free minimum rank. Core and named setting utility skills remain editable through the ordinary cap, charge only ranks above the grant, omit a paid overlay at the free floor, preserve higher paid ranks when the grant changes, and cannot be budget-repaired below the granted rank. Skills and Bonds widgets now disable only controls they own.
- Signed-in browser verification used historical character `oQvPBy4LCN6GxGYEGvKR`. Moving Academics from Rank 3 to 2 freed one point, Medicine then increased from its free Rank 1 to paid Rank 2, and Save remained enabled. On Bonds, blurring Background Keystone 1 preserved `keystone 1`, left both Keystone inputs and both Save actions enabled, and retained the unsaved granted-Bond Keystone text `Test`. The corrected Bonds tab remains open with those two unsaved values; no Save action or character write was performed.
- The final regression gate passed: `npm run test:all` ran 238 unit tests, 16 Firebase emulator Rules/persistence tests, and all 14 HTML entry points with zero failures. `npm run baseline:data` matched all nine frozen artifacts and `git diff --check` passed. The local review environment was restarted at PID `29792`; App: `http://127.0.0.1:5000`; Emulator UI: `http://127.0.0.1:4000`.
- Scope remains within the current top Work Package E feature commit. No production data, canonical Sheet cell, Firebase document, Rule, Function, or production Hosting target changed.

Prior Work Package E all-domain automated verification on 2026-08-12:

- `npm run test:all`: 223 unit tests, 16 Firebase emulator Rules tests, and all 14 HTML entry points passed with zero failures.
- The published-data regression reconciles every selectable class, every selectable weapon base, valid exact-budget attribute allocations at all 12 supported levels, and canonical Artifact/Patron Bond grants against `public/data/game-x/game-x-data.json`. Focused tests cover exact Bond/Keystone commands, centralized Rules, stable graph bindings, source ownership, deterministic caps/repair, derived ability removal, fixed-point idempotence, cancellation, page/widget/persistence independence, and Boon extension without page/traversal branches.
- `npm run baseline:data`: all nine frozen production artifacts matched. `git diff --check` passed.
- The local Firebase review environment was restarted and is running at PID `16516`. The Bonds/Keystones HTML, migrated page, portable widget, Bond Rules, and Boon graph/widget adapters all returned HTTP 200. App: `http://127.0.0.1:5000`; Emulator UI: `http://127.0.0.1:4000`.
- Browser testing previously exposed a mixed-version cached ES-module graph on the first returning-user load. JavaScript revalidation headers and versioned migrated entry modules remain in place. Manual acceptance is recorded as deferred and must not be prompted again until the user reports availability.
- Checkpoint scope is committed in the current top feature commit. No production data, canonical Sheet cell, Firebase document, Rule, Function, or production Hosting target changed.

Work Package B publish verification on 2026-08-09:

- The user approved and the agent applied exactly six `WeaponBases` source-cell corrections: the display and stable tag lists for Longsword, Shuriken, and Greatsword. Connector readback verified the values and preserved surrounding formatting.
- The user also approved executable Heavy prerequisites and `draft` selection mode for Gravity Weapon and Seismic Weapon; connector readback verified the six affected cells and preserved their validation and formatting.
- `npm run data:source:check` passed for canonical Drive version `647`, modified `2026-08-09T02:27:03.310Z`, using the dedicated Viewer identity through short-lived impersonation.
- `npm run stage:data` produced immutable run `20260809T022801911Z-51956` with zero errors, the same 28 intentional warnings, runtime-load acceptance, nine staged artifacts, and no production write. Model SHA-256: `c03ed329b10e27310baef32d73fe239c9146e415c332ae62f7842d81e9a17739`.
- Against the preceding candidate, only `weapon-enhancements.json` and the combined `game-x-data.json` changed, matching the approved source scope. The refreshed exact candidate is `20260809T022801911Z-51956`.
- Earlier runs `20260808T194536442Z-37004` and `20260808T194543102Z-38020` established that byte-distinct XLSX transports for unchanged Drive version `642` produce byte-identical runtime artifacts.
- The user approved the exact candidate and separately authorized `WPB-PUBLISH`. `npm run publish:data -- --confirm 20260809T022801911Z-51956` installed the nine exact artifacts and transactionally updated release provenance/baseline.
- Publisher tests cover approval mismatch/tampering, exact promotion and stale-artifact removal, and full restoration of production plus baseline after an injected mid-install failure.
- The schema-v2 release exposed and repaired legacy feat-category and grant-identity casing assumptions; compatibility aliases preserve existing source-owned choice answers.
- `npm run test:all`: 174 unit tests, 16 Firebase emulator tests, and all 14 HTML entry points passed. `npm run baseline:data` verified all nine schema-v2 production artifacts.
- The local Firebase review environment was restarted at PID 24108. An authenticated browser loaded the existing Magical Guardian class builder, class feats, Dazzling Wand, canonical Spellcasting technique choices, and stable-key technique options with no console errors. App: `http://127.0.0.1:5000`; Emulator UI: `http://127.0.0.1:4000`.
- `npm run deploy:hosting` released 100 public files successfully at `https://game-x-character-builder.web.app`; no Rules, Functions, or other Firebase target was deployed.
- External verification returned HTTP 200 for every runtime artifact; all nine deployed byte lengths and SHA-256 hashes exactly matched `contracts/game-data-release.json`. The deployed sign-in page loaded in the browser with no console errors.
- Checkpoint scope: the exact approved production JSON, compatibility loaders/identity handling, publisher, release contracts, baseline, tests, and living docs changed. No canonical-Sheet cell, Firebase Rule, Function, or Firebase document changed.

Verified at `WPD-GRAPH-CORE` completion on 2026-08-08, with the prior session, persistence, migration, read-only `WPB-SOURCE-RESOLUTION`, and Hosting-only release evidence retained below:

- `npm run test:all`: 169 unit tests, 16 Firebase emulator tests, and all 14 HTML entry points passed with zero failures.
- Twelve focused graph-core tests cover deterministic typed graph construction, stable ownership/storage bindings, collection-order stability, duplicate/dangling/cycle/missing-handler failures, fixed-point removal/capacity/prerequisite/incomplete policy, blocking-error separation, bounded non-convergence without partial state, transitive affected closure, exact session acceptance, side-effect-free cancellation, 40 generated convergence/idempotence/input-purity cases, and forbidden dependency imports.
- `npm run baseline:data`: all 9 frozen production artifacts matched; WPD-GRAPH-CORE changed no production game data.
- `git diff --check`: passed.
- The local Firebase review environment was restarted at PID 38004. `/js/core/graph-core.js`, `/js/core/graph-compiler.js`, and `/js/core/graph-reconciler.js` returned HTTP 200. App: `http://127.0.0.1:5000`; Emulator UI: `http://127.0.0.1:4000`.
- Checkpoint scope: fixture-driven pure graph modules, tests, and living contracts changed; no deployed page path, production JSON, canonical Sheet cell, Firebase document, Rule, Function, or production Hosting release changed.

- `npm run test:all`: 157 unit tests, 16 Firebase emulator tests, and all 14 HTML entry points passed with zero failures.
- Thirteen focused command/session/diff tests cover direct-intent validation, state isolation, deterministic exact-path diffs and impact ordering, one-pending-proposal identity, structured impact policy, side-effect-free cancellation, exact acceptance without rerunning reconciliation, invalid reconciled state, save-in-flight edits, and dependency purity.
- The five focused persistence emulator tests remain green for create/read, timestamps, migrated read without a write, explicit-save migration persistence, valid and invalid patches, stale-write preservation, missing documents, and authorization propagation; the other 11 emulator tests continue to cover Firestore and Storage Rules.
- `npm run baseline:data`: all 9 frozen production artifacts matched; WPC-SESSION changed no production game data.
- `git diff --check`: passed.
- The local Firebase review environment was restarted at PID 24476. `/js/core/character-session.js` and `/js/core/character-commands.js` both returned HTTP 200 and exposed the protected session, deterministic impact IDs, and typed command implementations. App: `http://127.0.0.1:5000`; Emulator UI: `http://127.0.0.1:4000`.
- Ten focused migration tests cover every evidence-backed edge, v2/future rejection, exact v5 codec acceptance, non-mutation/idempotence, metadata separation, primary-attribute restoration, stable reference resolution, deterministic IDs/reports, collisions, unresolved/ambiguous/unknown input, and module purity.
- `npm run baseline:data`: all 9 frozen production artifacts matched; WPC-MIGRATIONS changed no production game data.
- The local Firebase review environment was restarted (PID 48356) and returned HTTP 200 for `/js/core/character-migrations.js`; the served asset contains the supported-version registry. App: `http://127.0.0.1:5000`; Emulator UI: `http://127.0.0.1:4000`.
- Seven STAGING tests cover complete atomic output, production immutability, immutable run IDs, byte determinism, validation-error diagnostic-only runs, the validated-model construction gate, fresh runtime loading, split/combined equality, exact structural paths, stable semantic identities, the legacy technique bridge, and weapon-profile composite identity.
- ADAPTERS and REFERENCES fixtures remain green, including exactly 37 deterministic invalid-reference errors plus one warning after removing a false positive for optional composite-identity fields.
- Live `npm run data:source:check` was retried after the approved Sheet write and failed safely because the configured ADC lacks read-only access to the canonical Sheet; neither ignored XLSX nor provenance output exists.
- The exact `npm run stage:data` acceptance command stops at the same acquisition boundary before creating a run or touching production.
- Advisory connected-Sheet reads confirmed the 14 tab names, exact schema-v4 headers, metadata versions 4/2/2, and all 152 `Schema` declarations. These reads informed fixtures but do not satisfy repository live-source acceptance.
- `git diff --check`: passed.
- On 2026-08-05, an earlier detached local Firebase process did not persist after its command session ended, so the user authorized the documented production Hosting-only fallback. The current 2026-08-06 local process is the separately verified instance reported above.
- `npm run deploy:hosting`: Firebase Hosting released 91 files successfully at `https://game-x-character-builder.web.app`; no data-publish, Rules, Functions, or other Firebase target was deployed.
- External verification returned HTTP 200 for `/` and `/login.html`. The deployed `/js/core/game-data.js` was byte-identical to the local release file (SHA-256 `77d6ea85df004bbaf7ad0457a1542e9b73b9ce0f61197b6ca3a541ed91093726`).
- Checkpoint scope: the approved canonical-Sheet cells and dropdown validations changed; no production JSON, credentials, fetched workbook bytes, or repository staging artifacts changed.
